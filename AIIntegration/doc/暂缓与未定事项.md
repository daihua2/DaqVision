# 暂缓与未定事项

> 2026-09-17 起维护。定了的事项移到对应定案文档，本表只留暂缓与未定。

## 一、暂缓（用户定：先不做）

| # | 事项 | 暂缓前的状态 |
| --- | --- | --- |
| 1 | 安全帽剩余 4 项：模型作为工件导入；fp16、int8 模型先验证；生成带标注结果图；模型 AGPL-3.0 许可证能否商用 | 已提建议，未定 |
| 2 | ~~指针表、数字表、OCR 三个模块~~ | 2026-09-17 用户改为继续讨论，移出暂缓 |
| 3 | 大模型解释层：四种提示词、零样本诊断、频谱分析、能否出网 | 未讨论 |
| 4 | 功能缺口的做法：离线 CSV 导入、模型参数导出、标注导出 | 已定要做，做法未定 |

## 二、往来函记录

- **AI-59**（答 `H-260`，致 historystore 抄 AICloud）：2026-09-19 成文，★**2026-09-22 已补投分发点**（此前只在 `daqvision/doc` 下，分发点没有这封 —— hs 是从仓里的路径读到的，见 `H-262` 相关栏"原文在 `daqvision/doc/…`、未抄贵方"，AICloud 则一直看不到）。见 [doc/](../../doc/) 下 `2026-09-19-AI-59-*`。要点：定形态不定字段表（三件字段表只作示例、不注册不占名）；逐条结构 VQT；结构由采集侧定义、我方按字段绑定；请 hs 在字段元数据加物理映射描述（物理量、传感器坐标轴）；撤回 `AI-50 §2.3`；订正 `AI-56 §3.2`。★`AI-56 §3.2` 的订正**到此才真正送达**。
- **C-50 / AI-60 / AI-61**（`SOURCE_IDENTITY` 催办与三方上线顺序）：2026-09-22。AICloud `C-50` 催办两方 `AI-2 §3.2` / `C-2 §4.2` 09-10 定下的 `SOURCE_IDENTITY`（op 7 + `SourceIdentity.kind` + `EntityIdentity.sourceKind` + 能力位 `identity-source-kind`），起因是对账日志只有 guid、认不出写点方是谁。我方核过三项对得上（guid `db20ff1e…`、版本 `0.1.0+src20260919T135034Z`、点数 27）。
  - **AI-60**（致 historystore 抄 AICloud 与 daqgate）：请排期；★指出 `C-50 §3`「对 daqgate 零影响」**只在推送侧成立** —— 读侧若落同一张表，老消费方看到的那一行与真网关逐位相同，于是上线顺序不止 AICloud 一家；建议读侧默认不返回该行（`EntityIdentityReq` 加 `bool includeNonGatewaySources = 3`，缺省 false = 今天的行为）；★请 `isGateway` 对 op 7 的行**仍取 true**（那一位标的是"身份行 vs 实体行"，置 false 会把 `AI-2` / `C-2` 刚订正过的语义拧回去）；另问重报节奏与老化（我方兜底重推是 3600 秒，为点快照定的，不是为身份帧）。
  - **AI-61**（答 `C-50`，致 AICloud 抄 historystore 与 daqgate）：§4 三方上线顺序**全接**；`kind` 确认 `"ai-service"`；`name` 定为 `AI 集成服务(AISERVER)`（照 `C-2 §1` 那张 `HsIdentity` 表现有写法），★并说明 `GetInfo` **今天没有"服务名"这一格**，这个名字是函里新定的；★**补第 2.5 步**：第 2 步函告之前先用一次性 guid 合演一次（函告不等于验过 —— 若 `sourceKind` 没真回出来，我方那行正好命中"建网关"分支且删不掉）；★附警告：真误建了**不要**用 hs 的 `RemoveGateway` 去擦，那口是整台移出、连历史一起删且不可逆。
  - ~~我方待办：等 hs 投契约 + AICloud 函告上线 + 演练通过才落码~~ —— 已改为先落码、开关缺省关（见下）。`IDENTITY(5)` 前后都不发。
  - ★**后续（同日）**：`H-272` 答 `AI-60` —— hs 1.9.515 落地，三问全照我方：读侧**默认不回**（`EntityIdentityReq.includeNonGatewaySources = 3`，同一张表）、op 7 的行 `isGateway = true`、不老化；空 kind 拒收；老引擎静默吞 op 7 ⇒ 发方须探能力位 `identity-source-kind`。`C-51` 接第 2.5 步演练，并查出 daqgate「网关管理」读身份表、带「整台移出」。★`H-272 §5.2` + daqgate `D-267 §3`：daqgate 那张列表一半按**点表镜像 `GROUP BY guid`**，我方 27 点一旦有主控连上 AISERVER 的库就会带着「整台移出」出现（眼下 AISERVER 那台 daqgate 是从，不在；其 WSL 主控联调正在打通）；daqgate 区分的唯一依据是 `sourceKind`。`H-273`：1.9.517 修 0 点源整台移出误回 NOT_FOUND（契约不变）。`C-52`：平台第 2 步代码完成未发布；★§3 平台卡片墙上 `db20ff1e…` **今天就挂着「移出网关」**。`C-53 §5`：该按钮**直接调 `RemoveGateway`、不可逆**，界面要输入名字二次确认，已口头告知用户勿点。
  - **我方落码**：提交 `9051f3b` —— 契约重取至 1.9.515（1.9.517 契约零改动）；op 7 三道闸（开关 `AII_SOURCE_IDENTITY` 缺省 off / 能力位 / kind 非空），每条流最前发；749 条三环境全过、变异 8 发 8 红。**未部署，开关关**。
  - **AI-62**（致 AICloud 与 historystore 抄 daqgate，2026-09-22 20:52 投）：★请 AICloud 在代码里**按 guid 先挡住**卡片墙上 `db20ff1e…` 的「移出网关」（不只靠二次确认与口头告知）；提请我方第 3 步与平台"发到所有部署后函告"**解耦**，改为「现场 hs 升到 1.9.517、探得到能力位即发」（读侧默认不回 ⇒ 老代码看不见、新代码按 kind 不建）；请 daqgate 把「sourceKind 非空不挂移出」排在主控联调打通之前。已由 `C-54` 答复（见下）。
  - **C-54**（答 `AI-62`，2026-09-22 21:03）：★更正 `C-52 §3` / `C-53 §5` —— 平台卡片墙**今天并无** `db20ff1e…` 那张卡（09-20 起已摘除"无身份行的写入方"，生产与开发服务现取均无）。真口子在其第 2 步**初版**：我方发 op 7 后 `hasIdentity` 变真，卡会带着「移出网关」回来；已补（`caf60a1d`，判据改为「无身份行或自报了种类」，浏览器实测退回旧判据即复现）⇒ 不必按 guid 挡。★**§3.2 解耦：同意** —— 我方第 3 步 =「现场 hs 升到 1.9.517、探得到能力位即发」，不再等平台函告；老代码置不了 `includeNonGatewaySources`（`038a3583` 前的生成码无此字段）。请求：**打开开关那天函告**，平台当天看对账日志（代替第 2.5 步演练）。
  - **D-268**（daqgate 致 hs 抄我方）§4：daqgate「会在联调打通之前把入口处置掉」，已提请其用户拍板；按 hs 给的 8086 口自己数过 27 点逐行一致；AISERVER 升级窗口仍待其用户定。
  - **现状与下一步**：各方前置已齐，只差两件 —— ① 现场 hs 升 1.9.517（daqgate 用户定窗口）；② 我方部署 `9051f3b` 到 AISERVER（须用户授权）。开关 `AII_SOURCE_IDENTITY` 仍关；hs 升级后打开并当天函告 AICloud。
  - ★**2026-09-22 已打开**：实时库升 1.9.517 后，经用户授权部署 `9051f3b`（21:49，README §29）并打开开关（21:51:26，drop-in `source-identity.conf`）。日志 `accepted=30（含自报身份 SOURCE_IDENTITY）`；实时库侧现取：缺省请求 0 行、按 guid 点名 0 行、显式请求恰一行（`AI 集成服务(AISERVER)` / `ai-service` / `isGateway=true` / 自身 gid **2761**）。**AI-63** 当天函告 AICloud（21:52 投），请其看对账日志，并提醒第 2 步发布务必带 `caf60a1d`。★**待**：AICloud 第 2 步发布后回报对账日志；daqgate 用户裁定「sourceKind 非空不挂整台移出」（须在其主控联调打通前）。
  - **C-55 / C-56**（2026-09-22 22:00 / 22:33）：老代码下现场与今天相同；★更正 `C-54`：平台**生产没接**这个实时库，只有开发服务一套。平台第 2 步（含 `caf60a1d`）22:27 发开发服务，现场验过：对账日志「「AI 集成服务」…不是网关，未建网关实体」、镜像有我方行（gid 2761）、网关实体仍 2 台、卡片墙不列我方 ⇒ **平台一侧收尾**。
  - **C-57**（2026-09-22 23:38，征询）：我方 27 点归属链为空 —— 见 **§二之六**，**未定、未回函**。
- **AI-49**（告知振动两模块加停机判定）：2026-09-18 10:58 **已发出**（提交 `cb71068` 之后 8 分钟，本机随即故障，故仓里一直没记上）。不请求答复，但请 AICloud 界面做两件事：凭 `run_state` 把停机时的数值置灰、给 `stop_threshold` 出输入格。契约字段没动、不升版。
- **H-246**（historystore 致我方，抄 AICloud 与 daqgate，2026-09-18 01:41 到）：**订正 H-244 的建点建议** —— 波形/压装/谱能量改走「结构值」，图片/视频定为独立媒体接口。影响与我方答复见 [README §21](../README.md)。★**末尾四问待回，回函未发**。
- **C-47 / AI-57**（契约 1.10）：2026-09-19。AICloud 确认 1.9 齐备、六格全用上、四步向导已落地，
  但点名两件 **都是我方欠的**：★`Binding.points` —— 我方 `AI-41` 就许过这个出口却一直没落契约，
  没有它，看结论那一侧（数据面直连实时库）只有点号与质量码，按 `stop_behavior` 置灰**只能前端写死点名单**；
  ★质量码两套编号没对应关系 —— 字典里是语义码（字符串），写进库的是数值码。
  两件已补，**契约升 1.10**（`Binding.points` + `ConclusionPoint` + `QualityCodeInfo.status_code`），正本已投。
  ★**实现待投现场**。★对方没有拿点名单顶上，而是把结论三格做成**只读预览**并明写"置灰没做" —— 记下。
- **C-48 / AI-58**（三件"记录"的回复）：2026-09-19。
  ★**甲1 证实"现在没在攒"，且比我方预估严重**：库里只剩 **1.4 天**，三个不相干的点最早时刻**完全一致**
  ⇒ 是**整库在那一刻被清空**（上次盘触底删历史），而保留策略页面上写着"永久保留"、**不报错**。
  ⇒ **前置是容量，不是保留天数**（`RetentionDays` 挡不住触底清库，两者走的是不同的路）。
  我方已改规划，内部记为「**尚未开始**，前置是容量」，不再按"数据在积累"排 RUL。
  ★**甲2 我方原提法有缺陷**：写的是"设备上加一组字段"，而失效/更换天然是多条 ——
  对方改成**事件序列**是对的，做成标量格会**静默覆盖**掉前面几次，且丢的是训练标签、曲线仍然平滑。
  我方要的一条事件四格：时刻 / 类型 / 部位 / 说明；只求"可按时刻排序且不丢"。
  甲3 自由文本够用（类别 ≠ 同型）。
  ★另记一条对 RUL 直接相关的事实：**现场远程网关按作息关门关机，夜间断档是常态、开机不补传**
  ⇒ 轨迹里那一段段空洞是**真实工况**，不是采集丢数，不要当异常去补。
- **H-258 / AI-56**（hs 要一帧真实振动波形原始采样）：2026-09-19 已答 **「没有」**。
  ★理由不是"暂时没取到"，而是**现场在用的有人 USR-SVT10-01B 结构上就不产原始波形** ——
  它只出算好的标量（三轴速度/加速度/位移/频率 + 温度，Modbus 大端 16bit），
  原始加速度序列**不出厂**。能出的是维特 WT-VB02-485 的 `RAWFIFO`（1024 包 × 6 字节、
  int16 大端、三轴交错、`/32768×16g`、采样率 64Hz~32K 可配），**但我方没有那台设备**。
  ⇒ **S4 待的不是几天，是等换传感器**；波形数据源进展不在我方手上（要选型与采购）。
  ★函里另报一条：**对方那两张压缩收益表量的是 f32/f64，而真实采样是 i16 定点**，
  字节形态不是一回事（f32 有阶码/尾数结构，i16 是纯补码 + 量化噪声），
  合成 f32 上的 1.23~1.30× **不能直接外推**。我方不主张 S4 做不做，只提醒复量时形态要对。
- **AI-55**（投完函告）：2026-09-18 **已投实现并重启**，现场自述 `1.9`、`service_version` 带构建标识、
  六格逐条可取、`load_errors` 为空。模块 1/2 拆分的绑定已迁移（新建两条照搬旧的，再删旧的；
  **旧的 12 个结论点原样保留**）。★现场**只有 2 个域**：伺服与 VFD 因 venv 缺 `numpy`/`onnxruntime` 未投，
  且那两类现在投了也出不了结论。详见 [README §27](../README.md)。
  ★**仍待办**：`vibration_baseline` 需**重采基线**（要人选一段正常运行时间，我方替不了）。
- **C-46 / AI-53**（契约投了、**实现没投**）：2026-09-18。AICloud 重取 1.9 后**六格逐一相符、无不符可报**，
  但顺手拨了一次现场那台 `50070`，**自述还是 1.7**。核实属实且**比对方查到的更落后** ——
  现场 `domains/` 里是**拆分前**的 `vibration_lowfreq.py`，等于停机判定、伺服、VFD、契约 1.8/1.9 全不在。
  ★**根因不是忘了重启**：我方那条硬规矩「改即投」原文**只管契约**，而联调用的是跑着的那台，规矩没覆盖到。
  已改为**契约与实现同批投**。★**部署待授权**，投完须函告 AICloud。
- **AI-54**（三件"记录"）：2026-09-18 已发，致 AICloud 抄 historystore 与 daqgate。
  长期留存策略 / 失效事件台账（投运·失效·失效部位·**更换记录**）/ 同型设备标识。
  ★**这三件是全仓唯一"每晚一天就永久少一天"的一组**，52 封函里一次没提过，是我方疏忽。
  甲1 的机制已有（`H-251 §7`：hs 现在认 `Paras.RetentionDays`），缺的只是"对哪些点用、留多久"。
  ★函里第 4 问是要害：**若判断现在不做，请明确说** —— 我方最怕的是"以为在攒、其实没攒"。
- **C-45 / AI-52**（界面开工的六处缺格，契约升 1.9）：2026-09-18。AICloud 收下解除并给了三件的顺序，
  自查确认**前端没有按域名写死**；但点名两件 —— ★**我方 1.8 正本一直没投分发点**（违反硬规矩 5「改即投」，
  已补投且直接投 1.9）、★**`OutputSpec` 少一位使 `AI-49` 要的"凭 run_state 置灰"只能靠前端写死点名单**，
  即**我方 AI-49 与 AI-51 两封函自相矛盾**，由对方点破，我方认下。
  `C-45 §3` 六条缺格**全接并已落码**（见 [README §26](../README.md)），`AI-52` 已答。
  ★对方的排期口径：**不承诺到日**，由其用户临时定优先级；我方目前没有现场节点，**不催**。
  ★对方自陈：域概览页已按自述渲染、无欠账，但**五步向导尚未开工**（属被 `AI-40` 暂停的那批）——
  那部分是**承诺不是既成事实**，我方照此理解。
- **AI-51**（解除 `AI-40` 的界面暂停，请 AICloud 开工做界面）：2026-09-18 已发，附《四类诊断模块说明》。
  ★`AI-40` 那道停工令是**我方**下的（2026-09-16「界面将有较大重构，`C-39 §3` 那三件先别动手」），
  现在算法域已跑通 ⇒ 解除。函里另**订正 `AI-49` 一处笔误**：写的 `DescribeDomains` 不存在，实际是 `ListDomains`。
  **请对方回 5 问**：三件的排期、前端是否已按 `ListDomains` 渲染、`ListDomains` 缺不缺格子、
  常设录入责任是否算进新设计、"重构以设备为中心"落到什么程度（对不对得上 `ParamSpec.level`）。
- **AI-50**（答 `H-246` 四问）：2026-09-18 17:50 **已发**（用户授权直接发，不过目）。答：建点数为零、结构值对骨架无冲突但要补三格且必探能力位、波形数据源仍无、样例数据一有即送；并请对方知会 `1.9.464` 投放到现场的时点。**不请求答复**。
- **H-249 / H-251 / H-250**（historystore 致 daqgate，抄我方，2026-09-18 下午，在 `/home/Project/reference/`）：★**`struct-value` 与 `media` 两个能力位都已点亮**（`1.9.463` / 可调），`1.9.464` 已投至 daqgate `thirdparty/`，**投放窗口由 daqgate 排 ⇒ 现场此刻还没有**。详见 [README §21.6](../README.md)。**不需我方答**。
- **H-247**（historystore 致 daqgate，抄我方，2026-09-18 03:33 到）：与我方相关的是三条 —— 能力位已上 `PingRes.features`（我方探测就用它）、降级下限 `1.9.453`、`Paras` 的「缺键 ≠ 换绑」与「拒绝粒度是单个键」。**不需我方答**。

- **AI-44**（协商诊断标识 `binding` 取值、设备参数改后提示基线失效）：2026-09-17 投出后，用户以「方案未定、不发函」为由撤回，**已从分发点删除**，当时无来函引用。★更正：AICloud 实际已读到（`C-43` 末句提到「等我方答 `AI-44`」）；2026-09-18 已在 `AI-48` 告知已撤回、请勿作答。我方发文编号因此从 AI-43 跳到 AI-45。

## 二之二、结构值点（2026-09-18 定四条，另留 6 项未定）

`H-246` 把波形/压装/谱能量改成结构值后，命名与版本策略已定 ——
见 [结构值点定案.md](结构值点定案.md)：`AI_` 前缀 + 用途名、按用途一份全网共用、
运行期 `allowNewVersion` 恒 `false`、域在 `Declaration` 里用中立类型自述。
★**尚未落码**；该文 §三 另列 P1~P6 六项未定（发新版本的运维动作、注册时机、
具体字段表、坏点掩码、读侧声明落点、旧版本绑定的处置）。

## 二之三、第②层的抗漂移处置（2026-09-18 提出，**未定**）

来自 `research/scalar-capability` 的实测，**这三条从没进过任何定案**：

| # | 事 | 实测依据 |
| --- | --- | --- |
| D1 | ★**第②层没有抗漂移处置** —— 某一维只要有 **2% 的单调漂移**、而它的 σ 很小，`anomaly_score` 就会持续爬升，**看起来像劣化，其实是漂移**。实测：`y_acc_rms` 后半只比前半低 1.9%，光靠这一维就能 92.6% 认出前后半 —— **漂移不需要大，只要单调就够模型利用** | §2①、§3 |
| D2 | 现在的 `anomaly_score` 里，**故障占多少、漂移占多少从来没分开过** | §3 |
| D3 | 候选处置：**滑动近期基线** 或 **显式扣转速**（后者要有可信转速输入） | §3。★**2026-09-19 SCA 真实工厂数据实测**（[research/sca-scalar](../research/sca-scalar/README.md)）：两者都**只治漂移**；近期基线确能把误报压到多数 ≤0.11，扣转速几乎无效。★但更根上的一条：**速度有效值本就查不出轴承故障**（10 例基本分不开，包络 7 例分得开）—— 模块 2 的定位要重新写清（该文 §7，**未定**） |

★**与 R4「防抖维持暂缓」不是一回事**：滞回+连续计数治的是**抖动**（值在边界来回跳），
**治不了单调漂移**。就算将来做了防抖，D1 仍在。

★**未定**：是现在就给第②层加抗漂移处置，还是记着等现场真出现"缓慢爬升的误报"再说。
我方倾向后者 —— 现役基线是**手动重采**的（定案 5.2），重采一次即把漂移清零，
现场已有一条可用的人工兜底；且漂移与真劣化的分离本身需要现场数据来验。

★**已订正的两条**（原以为是漏项，核后不是）：
- ~~模块 2 的 13 标量分类"待重新裁定"~~ ⇒ **不是矛盾**。定案 2.3 定于 09-17 22:08，
  在实测（18:06）之后、知情后所裁；且实测推翻的是「标量信息量不足」（能力），
  定案的理由是「等现场有带故障标签的数据」（数据可得性），两者不冲突。
  [模块划分.md](模块划分.md) 里那条批注已订正。
- ~~向传感器厂家核实位移积分口径~~ ⇒ **现役模块不吃位移**（拆分后只吃速度与温度），
  对现役零影响；且不必问厂家，详见 research 那份的 §2③ 订正。

## 二之四、阶次谱能量的基频自证（2026-09-19 提出，**未定**）

阶次能量已裁定做结构、每轴一条（见 [结构VQT与波形数组定义.md](结构VQT与波形数组定义.md) §6.2），
遗留两件：

| # | 事 | 现状 |
| --- | --- | --- |
| E1 | ★**基频无法从数据自证**。45 个值都是「基频的 N 倍处」，基频由维特 `0x2A BASEBAND`（仁科为 `0489H` 起的倍频表）定；配置一改，数值照常、含义全变。采样率能由 T 自证，基频不能 | 候选：采集侧（daqgate）每次顺带回读 `0x29`/`0x2A`，与绑定参数不符即置坏 Q。**未与 daqgate 提** |
| E2 | 维特手册上位机截图 OCR 出现「7X」「8X」及疑似「速度谱能量」，寄存器表只到 6X、只有加速度谱 | 待向厂家核实，与标定口径、ERR 位定义同一批。我方无实物 |

★E1 不影响结构字段表（按位置命名，倍频与基频本就进绑定参数），只影响"配错能不能被查出来"。

## 二之五、压装改逐点结构 VQT 之后（2026-09-19，裁定已定、**代码未改**）

裁定见 [结构VQT与波形数组定义.md](结构VQT与波形数组定义.md) §6.3。落地还差：

| # | 事 | 现状 |
| --- | --- | --- |
| F1 | ★（09-19 改：按定义文档 §10「按字段绑定」重做，不再注册我方自定义的结构）**结构值的读侧在骨架里根本没接上**（本轮顺查出，**比压装本身更要紧**）：`fetch.py` 从不填 `Frame.structs`；`structreg` 在服务里**无任何调用方** —— 启动不注册、建点不带 `StructRef`、取数不解码；P5 定的 `supportsStructValue` 也没落在 `QueryHistory` 上 | `e6927f3` 的提交说明写「结构值点落码」，**实为只落了零件、没接线**；压装域用例全是手工构造 `Frame`，所以一直是绿的。★即便 `1.9.464` 投到现场，这条路也**一条数据都拿不到**。未修 |
| F2 | `press_fit.py` 按逐点结构重写：输入改为逐点结构 + 开关点；按 ON→OFF 时段逐次出结论；三段按时间切分；丢点按 >1.5 倍基准判 | 未改。**依赖 F1** |
| F3 | ★**模型吃哪几段**：原 v4 压装模型是在**合成数据**上训的（`原四项目调查`），三段都有之后喂哪段没有依据。倾向：模型只吃**压入段**（按位移重采，即经典的力-位移曲线；「压装不到位」也由压入段末端决定），保压与回程只进证据 | **未定** |
| F4 | 对外：向 hs 改口 `H-246`「一次压装 = 一个值」（我方 `AI-50 §2.3` 接受过）；向 daqgate 提开关点（ON/OFF **逐变化都存**，不许被压掉）与逐点结构 | **前半已了**：改口已在 `AI-59` 发出（`H-261` 全部接受）。**后半已成文、未发**：`AI-64` 草稿在 `daqvision/doc/2026-09-23-AI-64-…md`，开关点在其 §5.3，方案全文见 [标量点组装为结构点_网关侧方案](标量点组装为结构点_网关侧方案.md) |
| F5 | ★**标量点转结构点**（2026-09-23 新增）：现场 54 个点全是 OPC UA 标量，要结构值只能由 daqgate 组装。方案定了三种成组模式与 `Assembly` 键，并按规则 4/8 判出**本期只转压机「实时位移 + 实时力」**一对 | 方案见 [标量点组装为结构点_网关侧方案](标量点组装为结构点_网关侧方案.md)。`AI-64` 已发、`D-270` 已答、`AI-65` **已起草未发**。我方两处前提被 `D-270` 驳倒（`ScanRate` 对 OPC 通道无效；网关侧本无死区，该条已撤回） |
| F6 | ★★**现场这批点没有可信数据**（2026-09-23 只读实测）：12 个点最近 24 小时**好值为 0**，好值最后出现在 **09-22 05:57**，至今约 38 小时；全是 `-1000`/`-29`。与 `D-270 §3.3`（10 小时断 274 次）多半同根但更重 | 明细见方案 §10。★**排在采样率与组装器之前** —— 组装器做完也没东西可组。已在 `AI-65 §6` 第 1 件请 daqgate 并案 |
| F7 | ★同刻性**已量出**（方案 §10.3）：压机 `ad`/`af` 好值 **97.9% 逐位同刻**、p95 错开 0 µs ⇒ `M1` 基本成立，ε=1 ms 全覆盖。但**样本只有 95 条、来自两小时、压机基本没动**，证明不了压装动作过程中的同刻性 | 待链路修好后在真压装上重量。★另：振动 `x` vs `z` 只有 **1.8%** 同刻（错开中位 14.4 s），三轴若将来要转须单独查 |
| F8 | 压机字段的 **`unit` 无人能自证**：点表 `Unit`/`Convertor` 全空，daqgate 亦无法补（`D-270 §1.3`）。双方一致「确认不了就留空，好过写个编的」 | 我方绑定核对因此**只能按 `quantity` 核**，在绑定处记明"单位未知"。真值待现场 / PLC 给 |

## 二之六、结论点的上级结构 —— `C-57` 征询，**未定、未回函**（2026-09-23）

**起因**：AICloud `C-57`（2026-09-22 23:38）—— 我方 27 个点 `relationId = 0`、`containerId = 0`，`ancestorIds` 为空，
连来源自身 gid 2761 都没接上；将来配报警时 `GFolderId = 0`、`AncestorIds` 为空，即 `alarm.proto` 定义的「未算出（缺陷）」态。
问：挂上级，还是由实时库补链。

**用户定的原则（2026-09-23）**：**来源自己登记自己的结构** —— 对网关来说，注册「网关 / 连接 / 通道」是网关的职责；
hs 只合成最后一跳「最外层容器 → 来源自身 gid」。⇒ 我方**挂上级**，**不请 hs 为无通道的点加特判补链**。
★我方曾拟 `AI-64` 请 hs 补链（理由是"不硬搬网关概念 ⇒ 不推任何上级实体"），**推论过头、已作废、未发出**：
那条裁定管的是**身份**不借网关的 `IDENTITY` 帧，不是"来源不登记自己的层级"。

**为什么今天是空链**（读 hs 仓 `ccf208b`，只作背景，不作请求）：`EngineHost::normalizedParentOf` 对采集点取
`channelGidOfPoint`，`relationId <= 0` 时回 `0` ⇒ 按 `ancestry.h` 四档约定是「无父、显式到顶」⇒ 链在第一步合法终止、
`topId = 0`、不置截断；`alarmGFolderId` 同样取通道 ⇒ 0。链靠的是**来源推上来的通道 / 连接实体**逐跳上溯到族根 15 的那一层，
再由 hs 合成到来源自身 gid。★hs 拼链**只认族根 17（通道）/ 15（连接）**，其余族根走第 ④ 支「无父、到顶」。

**待定两件（用户：待定）**：

| # | 问题 | 候选 | 各自代价 |
| --- | --- | --- | --- |
| 1 | 登记哪几层 | 甲：服务 → **域** → **绑定** → 点（对齐 网关 → 连接 → 通道 → 点）；乙：服务 → **绑定** → 点（三跳，同 hs 自指标形态）；或先征询 AICloud 想怎么归组 | 甲多推 域 + 绑定 两种实体；乙每个（域×绑定）一个实体。现场是 2 个绑定（`vibration_baseline` / `vibration_iso` 各一个 `khb-f1-g1-v1`） |
| 2 | 上级实体的类别 | 族根沿用 15 / 17；或新立 AI 专用族根 | 15/17：hs 链**零改动**，但 AICloud / daqgate 按族根会把域 / 绑定显示成「采集连接 / 采集通道」；新族根：语义干净，但要 hs 拼链认它（改代码），类别号要与 daqgate / AICloud 登记（AICloud 已为 AI 服务单立过类别 `68`） |

**定了以后要做的**（现在不写，本仓规矩）：`hsclient` 快照里先推上级实体、点的 `RelationId` 指向它；hs 按内容去重，
重推即更新，点号不变；点表链读时现算、无须 hs 动作。然后回 `C-57`（`C-57 §4.3 / §4.4` 的答案随之而定）。

**在定之前**：`C-57` **欠着没回**（AICloud 在等）；这 27 个点**不配报警**（配了就是缺陷态出报警）。

---

## 三、原未定 9 项的裁定（2026-09-17）

| # | 事项 | 裁定 |
| --- | --- | --- |
| U1 | 诊断标识 `binding` 取什么 | **暂缓**，等与 AICloud 联调时再谈 |
| U2 | 回复 historystore `H-244` 末尾 5 问 | **我方不回**，用户去沟通 |
| U3 | `CLAUDE.md` 硬规矩 2 | **已改**：写 hs 的只有骨架；worker 可只读直连 |
| U4 | 契约新增项次序 | **输入项显示名最先**（卡前端配置界面），其余随实施顺序 |
| U5 | 跨帧状态实现 | 骨架为每条诊断保存一份状态：推理时传入、算完带回、由骨架存工作台库；模块不碰存储。★**2026-09-18 已落码**（`U9` 第 ③ 件，视频前置 `V8`），见 [README §22](../README.md)。仍未做且**有意不做**：把 `since` 透到界面要动契约，等第一个 stateful 域（视频）一起提 |
| U6 | 模块 1、2 拆分后命名与迁移 | 代码名 `vibration_iso`、`vibration_baseline`；**随拆分代码首次部署一起迁移**，迁移时重采基线 |
| U7 | 波形数据源 | **暂缓**；模块 3 先落地，无数据报「无数据」 |
| U8 | 告知 AICloud「ISO 机组类别只留两组」 | 要告知，**与 U6 部署同批发**（发函前仍须用户同意） |
| U9 | 实施顺序 | ✅① 输入项显示名（契约 1.8）→ ✅② 模块 1/2 拆分（含 8 项定案 + 停机判定）→ ✅③ 跨帧状态（2026-09-18，README §22）→ **④ 伺服、压装、变频器 ← 在做**（伺服 README §23、变频器 README §24 均 2026-09-18 已落码；**只剩压装**） → ⑤ 报告生成、故障分段 → ⑥ 模块 3 → ⑦ 视频。★④ 里的**压装**与 ⑥ 模块 3 要用结构值点：`struct-value` **2026-09-18 已点亮**（`H-249`），但 `1.9.464` **尚未投放到现场** ⇒ 可以落码，**启用前必须先探能力位**（见 README §21.6）。伺服与变频器不涉及 |

## 四、对照借鉴平台提出的 R1~R7 的裁定（2026-09-18，用户按我方建议定）

来源：[借鉴平台振动诊断编排与设置.md](借鉴平台振动诊断编排与设置.md) §6。定了要做的写进 [诊断配置流程定案.md](诊断配置流程定案.md)。

| # | 事项 | 平台做法 | 裁定 |
| --- | --- | --- | --- |
| R1 | 设备开停机判定 | 启停表达式判运行/停机/未知；采样值低于门槛不诊断 | **做**，见定案 1.6、2.5 |
| R2 | 频带 | 采集策略名写明频带（如 速度 2–1000 Hz） | **不设频带参数**，只在速度口径参数旁加提示，见定案 3.3 |
| R3 | 模块 3 轴系 ↔ 采集点绑定 | 每个轴系单独绑数据源，未绑不能诊断 | **做**，见定案 3.6 |
| R4 | 防抖（定案 1.4） | 时间窗内超限比例 / 连续 N 次 | **维持暂缓**；将来先做「连续 N 次」一种 |
| R5 | 阈值自动铺设与滚动更新 | 截尾平均 × 高低两侧四级系数；自适应按周期重算 | **不做**，维持定案 5.2 手动重采。模块 1 已按国标分级、模块 2 出异常分；平台自己也不对速度有效值自动铺设 |
| R6 | 诊断频度与结论推送 | 每日定时一次；结论按模板推消息 | **频度保持按节拍；推送不归我方**（纯后端，只写实时库点），可在下一封函顺带提 |
| R7 | AI 诊断与健康度两份模型 | 故障分类、剩余可用时长分开配 | 故障分类维持 2.3 暂缓；**剩余寿命不判成不做**，排在 U9 之后，先用公开全寿命数据集（IMS、XJTU-SY）做可行性预研 |
