# 暂缓与未定事项

> 2026-09-17 起维护。定了的事项移到对应定案文档，本表只留暂缓与未定。

## 一、暂缓（用户定：先不做）

| # | 事项 | 暂缓前的状态 |
| --- | --- | --- |
| 1 | 安全帽剩余 4 项：模型作为工件导入；fp16、int8 模型先验证；生成带标注结果图；模型 AGPL-3.0 许可证能否商用 | 已提建议，未定 |
| 2 | 指针表、数字表、OCR 三个模块 | 未讨论 |
| 3 | 大模型解释层：四种提示词、零样本诊断、频谱分析、能否出网 | 未讨论 |
| 4 | 功能缺口的做法：离线 CSV 导入、模型参数导出、标注导出 | 已定要做，做法未定 |

## 二、往来函记录

- **AI-44**（协商诊断标识 `binding` 取值、设备参数改后提示基线失效）：2026-09-17 投出后，用户以「方案未定、不发函」为由撤回，**已从分发点删除**，当时无来函引用。我方发文编号因此从 AI-43 跳到 AI-45。

## 三、未定（待讨论）

| # | 事项 | 背景 |
| --- | --- | --- |
| U1 | 诊断标识 `binding` 取什么 | 需与 AICloud 协商；AI-44 已撤 |
| U2 | 回复 historystore `H-244` 末尾 5 问 | 是否要强制只读角色；图片短期是否非用 bytes 点不可；按 16 MB/32 帧分页够不够；给不给样例数据；边界标记点方案是否接受 |
| U3 | `CLAUDE.md` 硬规矩 2 是否修改 | H-244 查实 worker 只读直连实时库不冲突；视频定案（V6）依赖它 |
| U4 | 契约新增项与次序 | 输入项显示名（待办 20）、模块分组、轻/重声明、触发方式（按节拍/按新值）、取某帧频谱、故障分段、报告生成 |
| U5 | 跨帧状态怎么实现（待办 24） | 视频定案 V8 与报警防抖都依赖它；须持久化 |
| U6 | 模块 1、2 拆分后的代码名称，以及现役配置何时迁移、重采基线 | 拆分已定 |
| U7 | 波形数据源 | H-244 §14 指出现有 MQTT 传感器链拿不到原始波形，请我方核数据源 |
| U8 | 是否把「ISO 机组类别只留两组」告知 AICloud | AI-45 写了「我方判级代码调整另行告知」 |
| U9 | 实施顺序 | 已定案的模块与功能先做哪些 |
