# 结构 VQT 与波形数组 —— 定义

> 2026-09-19 定。**这是地基**：它定错了，振动算法怎么实现都是错的。
> 全部论断以 FEMTO 真语料实测为锚（`D:\download\AIRef\datasets\femto`），不引推测。

---

## 0 两条定义

### 定义一：结构 VQT = **一个时刻**测到的多个具名量

```
VQT ┌─ T   时间戳 —— ★**一个时刻**，不是一段时间
    ├─ Q   质量码 —— 这条记录可不可信
    └─ V   值
         · 标量 VQT：一个数
         · 结构 VQT：一组具名字段（同一时刻的多路）
```

★**T 和 Q 仍归 VQT，不进结构。** 结构只装"这一刻的量"。

### 定义二：波形 = **一个连续时间段内、结构 VQT 的数组**

★**「帧」不是存储对象，是一段时间范围的查询结果** —— 是**视图**，不是**实体**。

```
按时间范围取 ──▶ [VQT, VQT, VQT, …]  ← 这就是一帧
                  ↑每条一个真实时刻
```

---

## 1 锚：FEMTO 实测（全部可复核）

```
datasets/femto/Learning_set/Bearing1_1/
├─ acc_00001.csv … acc_02803.csv     2803 个（加速度）
└─ temp_00001.csv … temp_00466.csv    466 个（温度，另一路，形态不同）

acc_00001.csv  2560 行，78,393 字节
  首行  9,39,39,65664,  0.552, -0.146   → 09:39:39.065664
  末行  9,39,39,165620, -0.134, 0.541   → 09:39:39.165620
  行距  39 μs = 25.6 kHz
  跨度  99,956 μs ≈ 0.1 s
acc_00002.csv  首行 09:39:49.065664     ← 与上一帧整隔 10 秒
```

⇒ **每 10 秒采一帧，一帧 2560 行**；那 2560 行落在这 10 秒的**前 0.1 秒**，其后静默 9.9 秒（占空比 1%）。

★**订正一处曾经写错的**：早前记的「一个轴承 3269 帧」是**文件总数**，把温度文件算进去了。
实为 **2803 个加速度帧 + 466 个温度文件**。

---

## 2 一行怎么映射

```
一行  9,39,39,65664, 0.552, -0.146
      └──────┬─────┘  └──────┬──────┘
         VQT 的 T        结构的 2 个字段
      09:39:39.065664   水平=0.552  垂直=-0.146
```

| CSV 的列 | 去哪 |
| --- | --- |
| 时,分,秒,微秒 | **VQT 的 T**（不进结构） |
| 水平加速度、垂直加速度 | **结构的两个字段** |
| —— | Q 由采集侧给 |

⇒ **一行 = 一个同时刻的完整采集记录 = 一条结构 VQT。**

---

## 3 由定义推出的六条（硬推论，不是偏好）

| # | 推论 | 为什么 |
| --- | --- | --- |
| 1 | **一个采样瞬间 = 一条结构 VQT** | T 是一个时刻，所以一条记录只能代表一个时刻 |
| 2 | ★**采样率不存，也不该存** | 相邻两条 VQT 的 T 之差就是采样间隔。另存一个 `sampleRate` 字段 = 制造**两个可能不一致的事实源**，元数据说 25.6 kHz 而 T 序列说别的，信哪个？ |
| 3 | ★**帧边界不存** | T 序列里的空档就是边界（FEMTO：连续 2560 条间隔 39 μs，然后跳 9.9 秒）。边界是**数据本身的事实**，不是需要另行声明的元数据 |
| 4 | **多路同步由"同一条记录"保证** | 水平与垂直在同一个 V 里，不可能各自漂。比两条独立标量时序强得多 |
| 5 | ★★**等间隔成为可验证的，而不是靠信任** | 算法拿到数组，**自己看 T 就知道等不等间隔** |
| 6 | **读侧一行都不用改** | hs 的 `QueryHistory` + `kRawData` 本来就是"给时间范围、回一串 VQT"，返回类型就叫 `VQTArrayRes` |

### 3.1 ★推论 5 为什么是决定性的

振动数据**不能用 SDT**（旋转门压缩）：它按值的形状丢样本，丢完**采样间隔就不等了**，
而 FFT、包络解调、阶次分析的前提全是等间隔。丢的还恰恰是峰值。

- 若把一帧塞进**一个** VQT：采样率是声明出来的。声明与实际不符时，**没有任何办法发现** ——
  曲线照样画得出，谱照样算得出，只是**全错**；
- 按本定义（**数组**）：采样间隔是 T 序列**量出来**的。`compErr`/`frozenSec` 若没设成 0 而丢了样本，
  T 序列立刻出现不该有的空档，**算法当场看得出**。

⇒ **把"必须等间隔"从一条靠人记住的配置纪律，变成一条数据自证的事实。**

---

## 4 ★本定义推翻了什么

| 被推翻的 | 出处 | 为什么错 |
| --- | --- | --- |
| 「一帧波形 = 一个值」 | `H-246 §2.1`；我方 `AI-50 §2.3` **已接受** | 那个 T 只对 2560 个采样中的**第一个**成立，另外 2559 个在 VQT 层没有自己的时刻 |
| `VibWaveform` 带 `wave` 数组 + `sampleRateHz` | `H-260 §2.1` 建议稿 | 同上；且 `sampleRateHz` 违反推论 2 |
| 我方拟的 `AI_VibFrame` 形态 | 本仓 `结构值点定案` P3 曾预留 | 同上（该结构**未落码**，只是预留，撤销无成本） |

★**我方需向 hs 发函订正** —— `AI-50` 接受过「一帧一个值」，现在要改口。

---

## 5 结构因此变得极小

```
VibSample（振动，双通道）        VibSample3（三轴）
  水平加速度  f32                  x  f32
  垂直加速度  f32                  y  f32
                                   z  f32
```

**没有** `sampleRate`、**没有** `channelNames`、**没有**帧长、**没有** `triggerReason`
—— 它们要么是数据本身的事实（推论 2、3），要么根本不是"这一时刻的量"。

★此前我方拟的字段表越写越臃肿，根因是**在给一个不该存在的实体（帧）设计元数据**。

---

## 6 一处例外：事件型数据

| 类型 | 一条 VQT 是什么 | 例 |
| --- | --- | --- |
| **连续采样** | **一个采样瞬间** | 振动波形、温度 |
| **事件型** | **一个事件** | 一次压装 |

压装曲线的横轴是**位移不是时间**；"这一次压装"确实是一个时刻发生的一件事，
整条曲线是**这个事件的属性**。⇒ `PressCurve` 做成一个值**说得通**，波形做成一个值**说不通**。

★判据：**这一条记录的 T，对它装的全部内容都成立吗？**
- 波形帧：不成立（2559 个采样各有各的时刻）⇒ 必须拆
- 一次压装：成立（事件发生在那一刻，曲线是它的属性）⇒ 可以整装

---

## 7 唯一要盯住的代价

逐行一条 VQT，记录数是大的：FEMTO 节拍下 **256 条/秒/传感器**（2560 行 ÷ 10 秒）。
每条要带 TagId + T + Q + StructId + 版本，而**结构值吃不到 Gorilla 列式压缩**
（hs 的 `gorilla_codec` 只处理 R8/R4/整数，结构值退到 gzip）。

★**这不是否定本定义的理由**（语义正确优先），但它是**唯一要盯的一处**，且有得谈：
可请 hs 评估结构值能否也走列式编码 —— 同一结构、同一质量的一段连续记录，
字段逐列压缩的条件是具备的。

---

## 8 验证结果（2026-09-19，FEMTO 真语料，四条全过）

脚本 `research/struct-vqt-verify/verify.py`，对 `Bearing1_1` 实跑：

| # | 验什么 | 结果 |
| --- | --- | --- |
| ① | 帧能否**只凭 T 序列**浮现 | ✅ 把 3 个文件拼成一条 7680 条的序列再切，**切回 3 帧 × 2560 条**。★全程未使用采样率/帧长/文件名等任何元数据 |
| ② | 采样率能否从相邻 T 推出 | ✅ 推出 **25,601.3 Hz**，标称 25,600，**偏差 0.005%**；帧周期推出 **10.0 s**，与标称一致 |
| ③ | 丢样本能否当场发现 | ✅ 原帧 2560 条、异常间隔 **0** 处；模拟 SDT 按值抽稀后剩 2349 条（丢 211），异常间隔 **192** 处 —— **当场暴露** |
| ④ | 代价落成实数 | 本轴承 2803 帧 × 2560 = **7,175,680 条**结构 VQT，覆盖 7.78 小时 ⇒ **256 条/秒**，净载荷 **57.4 MB** |

### 8.1 ★一处被真语料纠正的（写下来，免得再犯）

第一版脚本用**众数**推采样率，得 25,000 Hz，**偏 2.3%，验证不过**。查间隔分布才明白：

```
间隔分布(μs)： 40×1575,  39×825,  30×158,  41×1
平均间隔 39.0606 μs → 25,601.3 Hz
```

真实间隔是 `1/25600 = 39.0625 μs`，而**时间戳只有 1 μs 分辨率** ⇒ 必然在 39/40 之间抖动；
另有 158 个 30 μs 的**时钟毛刺**（真语料里本来就有）。

⇒ **采样率是整段的性质，不能用单个间隔去代表。** 取整帧平均（首末跨度 ÷ 间隔数）才准。
★同理，"找空档"的基准也要用平均 —— 否则 1 μs 分辨率造成的正常抖动会被报成异常。

**这一条对实现有直接约束**：将来算法要核对等间隔时，判据必须是"**相对整段平均**的显著偏离"，
不是"相邻间隔是否恒等"。后者在任何真实时间戳上都不成立。

### 8.2 ⇒ 结论

**帧、采样率、帧周期三样，全部是数据自证的**，不需要任何一个元数据字段来声明。
而 §3.1 那条最要害的（丢样本可发现）也验住了 —— 这正是「一帧一个值」做不到的。
