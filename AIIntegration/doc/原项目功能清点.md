# 原项目功能清点 —— 确保集成后不缺功能

> 2026-09-17。方法：只读列出四个原项目的**全部对外接口**（共 214 个），按功能归类，逐项对照我方现有接口与已定方案。
> 接口清单为【实测】；个别功能含义由接口名与实现片段判断，已在表中注明。

| 项目 | 接口数 | 来源文件 |
| --- | --- | --- |
| v4 振动/伺服/机械臂/压装 | 85 | `ai_diagnosis/v4/backend/app.py` |
| v5 低频振动 | 92 | `ai_diagnosis/v5/backend/app.py` 31 + `routes_realtime.py` 36 + `routes_knowledge_bases.py` 18 + `routes_sensors.py` 7 |
| VFD 变频器 | 27 | `VFD/app/backend/app.py` |
| 视觉（安全帽/指针表/数字表/OCR） | 10 | `meter_service/*` |

**状态说明**：✅ 我方已有　📋 已在方案里（未实现）　❌ **方案里没有，需定夺**　⛔ 不归 AI 服务（注明归谁）

---

## A 仿真数据与采集启停 —— ⛔ 不归 AI 服务

| 原功能 | 出处 | 归属 |
| --- | --- | --- |
| 演示数据回放（mini-demo、order-sample-demo） | v4 | 仿真发生器，已查实原项目数据均为合成，不移植 |
| 伺服/机械臂/压装模拟采集启停 | v4 `/api/servo|robot-arm|press/start|stop` | 同上 |
| 采集启停与采集参数 | v5 `/api/acquisition/*`、`/api/sensors/*/start|stop` | 采集归 daqgate |
| 会话启停、暂停、**故障注入** | VFD `/api/sessions/start`、`/pause`、`PATCH /fault` | 仿真发生器 |
| ❌ **离线 CSV 导入后诊断** | v4 `POST /api/sensors/{id}/import-csv` | **需定**：现场历史文件要不要能导入诊断 |

## B 设备、传感器台账 —— ⛔ 归 AICloud / daqgate

| 原功能 | 出处 | 归属 |
| --- | --- | --- |
| 设备增删改查 | v4、VFD `/api/devices` | 设备实体归 AICloud（已定） |
| 传感器/测点增删改查、激活 | v4 `/api/sensors`、v5 `/api/sensors`、`/api/realtime/sensor-configs` | 采集点归 daqgate / hs |

## C 历史数据浏览 —— ⛔ 归 hs + AICloud 界面

| 原功能 | 出处 | 归属 |
| --- | --- | --- |
| 时间线、按时刻/编号取帧、最新帧、会话列表 | v4 `/timeline`、`/frames`、`/frame-at`、`/latest-frame` | hs 历史查询 + AICloud 界面 |
| 实时/历史曲线（内存、CSV、数据库三种来源） | v5 `/api/realtime/history/*`、`/latest`、`/hs-history` | 同上 |
| 设备历史、单点历史、实时值 | VFD `/history`、`/history/point`、`/live` | 同上 |
| ❌ **单帧频谱图数据** | v4 `GET /api/sessions/{id}/frame-spectrum` | **需定**：由模块 3 输出频谱，还是 AICloud 自己算 |

## D 振动诊断算法

| 原功能 | 出处 | 状态 |
| --- | --- | --- |
| ISO 烈度判级 | 原项目**没有** | ✅ 我方新增（模块 1） |
| 基线偏离、温升、异常分 | 原项目**没有** | ✅ 我方新增（模块 2） |
| 阶次规则卡诊断（五类） | v4 `/diagnosis/order/rule`、v5 `/diagnosis/rule` | 📋 模块 3（要波形） |
| 手写 MLP 诊断（"轻量小模型"） | v4 `/diagnosis/order/mlp`、v5 `/diagnosis/lightweight` | 📋 模块 3 |
| 阶次模型训练 | v4 `/order-diagnosis/train` | 📋 模块 3 |
| ❌ **规则诊断与模型诊断结果对比（含导出 CSV）** | v4 `/order-diagnosis/compare`、`/compare-csv` | **需定** |
| 模板预警（留一法定阈值）及预警记录 | v4 `/diagnosis/template-warning`、`-records` | 📋 模块 3；预警记录 = 结论点写入 hs ✅ |
| 13 标量五类分类：训练、预测、清空、模型加载/删除 | v5 `/lowfreq/*`、`/realtime/model/*`、`/realtime/models/*` | 📋 模块 2 故障分类，**已定先不做**（2.3） |
| 知识库（训练集）增删改、复制、成员 | v5 `/api/knowledge-bases/*` | ✅ 我方 `PutDataset/CopyDataset/AddSamples/RemoveSamples` |
| 知识库下的模型列表、训练、删除 | 同上 | ✅ 我方 `ListArtifacts/StartTraining/DeleteArtifact` |
| ❌ **导出模型参数** | v5 `/models/current/export-parameters`、`/models/{id}/export-parameters` | **需定** |
| 传感器 ↔ 知识库绑定 | v5 `/api/knowledge-bases/bindings/*` | ✅ 我方 `PutBinding` |

## E 标注

| 原功能 | 出处 | 状态 |
| --- | --- | --- |
| 标签列表、单条/批量/按时间段标注、改、删 | v5 `/api/realtime/labels`、`/annotations*` | ✅ 我方 `ListLabels/PutAnnotation/AnnotateRange/DeleteAnnotations` |
| 取某传感器的训练数据 | v5 `/annotations/training-data/{id}` | ✅ 我方 `ListSamples` |
| ❌ **标注导出** | v5 `GET /api/realtime/annotations/export` | **需定** |

## F 片段归档与片段诊断

| 原功能 | 出处 | 状态 |
| --- | --- | --- |
| 片段归档增删查 | v5 `/api/realtime/archives*` | ✅ 我方 `ListSegments/PutSegment/DeleteSegment` |
| 对片段重新诊断（v5 为均匀取 ≤10 帧概率平均） | v5 `/archives/{id}/predict` | ✅ 我方 `RediagnoseSegment` |
| 对片段临时识别、不落库 | VFD `/sessions/{id}/model-recognition` | ✅ 同上：`RediagnoseSegment` **不写回实时库**【实测】 |
| ❌ **故障分段表**：对最近 N 秒逐窗诊断，把连续同类合并成一段 | v4 `/api/servo|robot-arm|press/{id}/fault-segments` | **需定**（伺服、机械臂、压装都有） |

## G 报告

| 原功能 | 出处 | 状态 |
| --- | --- | --- |
| 多条诊断合并生成 PDF 报告、下载 | v4 `POST /api/reports`、`/reports/{id}/download` | ❌ **我方未实现**：只有 `reports` 登记表与 `ListReports`，`add_report` 全仓无调用，没有任何代码生成报告【实测】 |
| 片段基础报告、深度报告 PDF | v5 `/archives/{id}/report.pdf`、`report-deep.pdf` | 同上 |
| 会话报告 | VFD `/sessions/{id}/report` | 同上 |

## H 大模型（LLM）

| 原功能 | 出处 | 状态 |
| --- | --- | --- |
| 基础诊断、深度诊断、基础分析、深度分析（四种提示） | v4 `/order-diagnosis/llm/*`、`/diagnosis/order/*-llm-*`；v5 `/diagnosis/basic-llm|deep-llm` | 📋 跨模块解释层（**出网待议**） |
| 零样本诊断（频谱图喂多模态模型） | v4 `/diagnosis/zero-shot`、`/prepare-gpt-image` | 📋 同上 |
| 大模型频谱分析 | v4 `/diagnosis/llm-spectrum-analysis` | 📋 同上 |

## I 伺服、机械臂、压装、变频器

| 原功能 | 出处 | 状态 |
| --- | --- | --- |
| 伺服轴承退化诊断（CNN-LSTM / 决策树 / SVM） | v4 `/diagnosis/servo/bearing` | 📋 模块 4（树模型路耦合 `services`，需拆） |
| ❌ **机械臂故障诊断**（复用伺服模型） | v4 `/diagnosis/robot-arm/fault` | **需定**：不在六个界面选项里 |
| 压装故障诊断（SVM / CNN-LSTM） | v4 `/diagnosis/press/fault` | 📋 模块 6 |
| 变频器诊断：6 类故障 + 当前严重度 + **30/60/120 秒后严重度** | VFD `/sessions/{id}/diagnose` | 📋 模块 5（**输出须保留未来严重度三项**） |
| ❌ **同一模块多模型怎么选**（伺服 3 个、压装 2 个，原项目按请求参数选） | v4 上述接口的 `model` 参数 | **需定** |
| 模型信息、数据集信息 | VFD `/api/model/info`、`/dataset/info` | ✅ 我方 `ListArtifacts`、`GetInfo` |

## J 图像识别

| 原功能 | 出处 | 状态 |
| --- | --- | --- |
| 安全帽/背心检测（上传图片） | helmet `POST /api/detect` | ✅ 已落码未上现场（模块 7） |
| ❌ **摄像头画面逐帧检测、不存图** | helmet `POST /api/live-detect` | **需定**：实时视频检测要不要 |
| 指针表读数、数字表读数、通用 OCR | pointer/digital `/api/read`、ocr `/ocr` | 📋 模块 8/9/10 |

## K 运维

| 原功能 | 出处 | 状态 |
| --- | --- | --- |
| 健康检查 | 全部 | ✅ 我方 `/health`、`GetInfo` |
| 日志 | —— | ✅ 我方 `SubscribeLogs/QueryLogs`（原项目没有） |
| 存储占用、数据保留清理预览与执行 | VFD `/api/storage/info`、`/maintenance/retention*` | ⛔ 原始数据归 hs；我方工作台库的清理**需核** |

---

## ⇒ 需要定夺的缺口（11 项）

| # | 缺口 | 出处 |
| --- | --- | --- |
| 1 | 离线 CSV 导入后诊断 | v4 |
| 2 | 单帧频谱图数据由谁出 | v4 |
| 3 | 规则诊断与模型诊断结果对比（含导出） | v4 |
| 4 | 导出模型参数 | v5 |
| 5 | 标注导出 | v5 |
| 6 | 故障分段表（伺服、机械臂、压装） | v4 |
| 7 | 机械臂故障诊断（不在界面选项里） | v4 |
| 8 | 同一模块多模型怎么选 | v4 |
| 9 | 摄像头画面实时检测 | 视觉 |
| 10 | **报告生成**（诊断合并报告、片段基础/深度报告、会话报告）：我方未实现，只有登记表 | v4/v5/VFD |
| 11 | LLM 解释层（四种提示、零样本、频谱分析） | v4/v5（出网待议） |

---

## 用户裁定（2026-09-17）

**上表 11 项缺口全部实现。** 原项目已有的功能，集成后一项不缺。

其中需要进一步定方案的（怎么实现）：第 2 项频谱数据由谁出、第 7 项机械臂在界面上怎么呈现、第 8 项多模型怎么选、第 9 项实时检测挂在哪、第 11 项大模型出网方式。
