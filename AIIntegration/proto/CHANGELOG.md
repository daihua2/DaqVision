# aiintegration.proto 变更记录

> 规矩：**字段编号只增不改不复用**；废弃用 `reserved`；改契约必须升 `proto_version` 并记在这里，
> 且**改即投分发点并发函**（AI-9 §2.3、C-9 §2.1）—— 不投就会出现"我方以为改了、贵方发版还是旧的"，
> 而且不报错。

## 1.10 —— 2026-09-19

**动机**：AICloud `C-47` 点名两件，**两件都是我方欠的**，不是对方做法不对。

★**第一件是旧账**：`Binding.points` 这个出口，我方在 `AI-41` 里就许过
（"结论点名不承诺，改给结构化出口"），**一直没落进契约**。
后果由 `C-47 §3` 说破：看结论那一侧走的是**数据面直连实时库**（趋势/组态图读点），
那条路上只有点号与质量码，**没有「域/绑定/结论 key」** ⇒ 对方要按 1.9 新加的
`OutputSpec.stop_behavior` 把停机时的数值置灰，**只能在前端写死一张点名单** ——
而那正是我方 `AI-51 §3` 反对、`AI-52 §3` 自己也认下不该做的做法。
对方没有硬做，而是把结论那三格做成**只读预览**并如实说明"置灰没做"，等这一格。

| 改动 | 说明 |
| --- | --- |
| `Binding.points = 10`<br>新增 `ConclusionPoint` | 本条绑定产出哪些结论点 + 各自 `local_id`（形状按 `AI-41` 许诺的那个：`key`/`local_id`/`name`/`unit`/`value_type`）。平台拿 `(AI guid, local_id)` 走 hs `LookupGlobal` 铸 globalId。★**`local_id=0` = 尚未建点，不是可用点号**，拿它去查会静默查到别人的点 |
| `QualityCodeInfo.status_code = 5` | 1.9 那张字典的 `code` 是我方**语义码**（字符串），而结论点写进实时库带的是**数值码** —— 两套编号此前在契约里没有对应关系，于是"按 `is_fault` 区分不是设备故障"这条规矩，在**真正会被现场看到的那个界面**上落不了地。这一格就是那张对照 |

★**`ok` 对应 `1`（`daq.StatusCode.Ok`），不是 0。**
  我方起初在本条的契约注释里写成了"对应 0"，**被用例当场逮到并已订正** ——
  写错的话，对端会把正常值按 0 去比，而 0 在那张枚举里不是"正常"。

**兼容性**：纯新增字段，不动任何已有编号。老客户端读不到新字段即为空/0，行为与 1.9 相同。
★`Binding.points` 在未接点表时**回空**，不是回一串 `local_id=0` —— 空 = "我方答不出"，
一串 0 会被读成"点号就是 0"。

## 1.9 —— 2026-09-18

**动机**：AICloud `C-45 §3` 逐条点名的**六处缺格**，我方**全接**。
六条的共同点是同一件事：**这些是域知道、界面不知道的事**，不放进自述，
前端就只能写死；而写死的代价对方也写明了 —— 我方加一个参数/结论点/取值，
界面**不会自己长出来，而且不报错**。

★**其中 `OutputSpec.stop_behavior` 那条是订正我方自己的矛盾**：
`AI-49` 要求界面"凭 `run_state` 把停机时的数值结论置灰"，
`AI-51 §3` 又立规矩"不要按域名写死" —— 而 `OutputSpec` 里根本没有这个信息，
前端只能把点名单抄进去。**两封函自相矛盾**，由 `C-45 §3.4` 点破，本版兑现。

| 改动 | 说明 |
| --- | --- |
| `InputSpec.group = 7`<br>`InputSpec.group_display = 8` | **成组可选**：同组角色语义是「全配或全不配」。由来：振动的「第二测点 X/Y/Z 速度+温度」四个角色各自 `required=false`，界面只看 `required` 无从知道它们是一组 ⇒ 用户配了 X 漏了 Z 不被拦，我方拿到半组输入 |
| `ParamSpec.has_default = 11` | ★`default` 用空串表达"无缺省"，与"缺省就是空串"撞在一起。`false` ⇒ **必须由人填，界面不要替它预置任何值**（`stop_threshold` 替它填一个"看起来合理"的数会**静默停止诊断**）|
| `ParamSpec.blank_meaning = 12` | **留空会发生什么**。已用取值 `not_evaluated`。不是封闭枚举 |
| `ParamSpec.min/max/step = 13/14/15` | 取值范围，给界面**当场拦截**用 —— 没有它，用户填 `-1` 要到采基线才失败，中间那段看着像配好了 |
| `OutputSpec.stop_behavior = 6` | ★★**停机时这一条怎么写**：`written`（照常写实测值）/ `literal_stopped`（写表示停机的取值）/ `not_written`（**这一拍不写**，点上保留停机前最后一值 ⇒ 界面据此置灰并标注时刻）。空 = 按 `written` 理解 |
| `OutputSpec.choices/choice_displays = 7/8` | **结论点的取值域自述**。由来：`iso_zone_code` 从 {1,2,3,4} 变成 {0,1,2,3,4}（0=停机），取值域只写在函里，每次变化都要发一次函 + 改一次前端 |
| `InfoReply.quality_codes = 7`<br>新增 `QualityCodeInfo` | **质量码字典**（`code`/`display`/`is_fault`/`hint`）。★`is_fault` 是这张表存在的理由：`no_input`（没取到数）与 `model_not_loaded`（没启用模型/基线）**不是设备故障**，混成"异常"会让现场去查设备，而实际要查的是采集链路或工件页。放 `GetInfo` 而不是每个域回一份：质量码是**骨架级**的，不随域变 |

**兼容性**：**纯新增字段，不动任何已有编号**。老客户端读不到新字段即为空/`false`，行为与 1.8 相同。
`stop_behavior` 空串按 `written` 理解，故老域（没标这一位的）语义不变。

## 1.8 —— 2026-09-17

**动机**：两个都是**卡着贵方出配置界面**的字段。

1. **输入项没有显示名**：贵方渲染"选采集点"表单时，只能显示 `x_vel` 这种角色标识符；
2. **参数没有归属**：机组类别、支承方式这类是**设备固有属性**，应在设备上填一次、各诊断共用；
   而轴向是哪一轴、速度口径随每条诊断走。没有归属，贵方只能把全部参数塞进每条诊断里重复填。
   本条是贵方 `C-40 §3.2` 的请求，我方 `AI-41 §2.1` 已接受，本版兑现。

| 改动 | 说明 |
| --- | --- |
| `InputSpec.display = 6` | 给人看的名字，如「X 轴速度」。空 = 域未提供，请回退显示 `role` |
| `ParamSpec.level = 10` | `machine` = 设备固有属性，设备上填一次、同设备各诊断共用；`position` = 随这条诊断填；空 = 域未声明，我方不替它猜，贵方可按 `position` 渲染 |

★**`Binding.params` 的形状不变**：贵方界面按 `level` 分处填写，**下发前合并成一份**；我方只收合并后的结果。

**兼容性**：纯新增字段，不动任何已有编号。老客户端读不到新字段即为空串，行为与 1.7 相同。

## 1.7 —— 2026-09-16

**动机**：★**我方在现场撞出来的一个真缺口，不是设计推演** —— 此前**只有"启用"没有"停用"**。

`ActivateArtifact` 只让**同作用域**（域,种类,对象）的旧件让位。于是**作用域本身建错**的那个件
没有任何办法取消：同作用域里没有别的件能顶掉它，而 `DeleteArtifact` 又拒删激活中的。

怎么撞上的：2026-09-16 重训基线时 `StartTraining` 的 `binding` 传了空串，
工件落成 **"全域通用"**（`binding=''`）并被启用。后果是静默的 ——
`artifactcache.for_binding()` 找不到对象专属件时**会回退捡这条全域通用件**，
于是该域**日后任何新绑定，只要自己没训过基线，就会套用另一台机器的基线出第②层结论**，
而本该出现的 `MODEL_NOT_LOADED(-1034)` 不会出现。

| 改动 | 说明 |
| --- | --- |
| `rpc DeactivateArtifact (IdReq) returns (MutateRes)` | 停用一个工件。`ok=true, id=1` 本来是激活的 / `id=0` 本来就没激活；没这个工件 `ok=false` |
| 命令行 `deactivate-artifact --id N --yes` | 与 `activate-artifact` 同规矩，**必须带 `--yes`** |
| 命令行 `list-artifacts` 多显示一列 | 训练数据来源（1.6 的 `data_origin`）—— 空显示**「未声明」**而不是留白 |

★**停用后该对象没有可用工件，结论会落 `-1034`，这是对的**：没有基线就该说没有基线，
不是退回"随便挑一个最新的"。界面上请按"会让该对象停止出第②层结论"提示。

**兼容性**：纯新增一个方法，不动任何字段编号。老客户端不调它即可。

## 1.6 —— 2026-09-16

**动机**：AICloud `C-36 §4` 请求，源头是我方 2026-09-16 训出的第一条基线 ——
那条基线**采自实时库的仿真信号**（`C-35 §4.4`、`AI-37 §4`），而"它是仿真的"当时只记在
**工件名、训练数据说明、标注备注**这三处**给人看的字符串**里。
字符串一改名就断，而「这条结论是不是拿仿真数据训出来的」恰恰是**要挡在人眼前的判据** ——
与 1.5 给 `origin` / `license` 单立字段是同一个道理。

| 改动 | 说明 |
| --- | --- |
| `Binding.data_origin = 9` | **这组点接的是仿真信号还是现场工况**。`simulated` / `field` / 空；不是封闭枚举，按字符串透传 |
| `Artifact.data_origin = 22` | 训练数据的来源性质。**只读**（我方写）：训练产出按样本快照**取最严**，外部导入按导入时声明 |
| HTTP `POST /artifacts/import/{domain}` | 多一个**可选**字段 `data_origin`（不填 = 未声明） |

**★为什么记在绑定上，而不是记在每条标注上**（2026-09-16 用户裁定）：
**仿真与否是点的性质** —— 绑的那组点接的就是仿真信号源，与人框了哪一段无关。
绑定声明一次，标注/样本/工件自动继承，**人没有机会漏填某一段**。
样本**入集那一刻打快照**（照 `Sample.label` 那条既有规矩）⇒ 日后真机接入、
同一绑定改成 `field`，**不会反写**当初用仿真数据训出来的工件。

**三条语义**（`C-36 §4.2`，两侧议定，改动要发函）：

1. **空 ≠ 现场。** 1.6 之前产出的工件读出来都是空，界面按「未声明」显示、与 `field` 用不同样式，**不许当现场**。
2. **取最严。** 训练集里混了仿真与现场，工件那一格记 `simulated`。混了多少比例照旧写在 `training_data` 里。
3. **不是工件的好坏。** `simulated` 不是"差的工件"，跑链路、做回归都该用它。它只回答一件事：**这条结论能不能用来说现场设备的话。**

**兼容性**：纯增量。老绑定与老工件这一格都是空 = 未声明 —— 按第 1 条处置，**不要在渲染时退回 `field`**。
★**不做自动回填**：按绑定"现在"的取值去回填历史样本，方向是错的 —— 日后绑定改成 `field`，
会把当初用仿真数据训的工件洗成现场。老数据就是未声明，如实显示。

★**一处例外，且只朝严的方向**：样本快照为空、而绑定当前声明 `simulated` 时，训练按 `simulated` 计。
理由：这条兜底**不可能**产出一个假的 `field`（绑定说 `field` 时快照为空仍记未声明），
只可能把"不知道"抬成"仿真"。现役那条基线（工件 id=1、训练集 id=1，样本入集于 1.6 之前）
正是靠这条在重训后拿到 `simulated`，而不必伪造历史快照。

## 1.5 —— 2026-09-12

**动机**：视觉域要用的模型是**外面训好的**，至今工件只从训练产出、没有导入的路。
同时对原项目取证发现：原 VFD 的模型是拿代码合成的数据训的、服务器上连训练代码都没有 ——
代码救不了这种模型，但能让模型**带着来历进来、并且看得见**。

| 改动 | 说明 |
| --- | --- |
| `Artifact.origin = 18` | `trained` / `imported`（不是封闭枚举） |
| `Artifact.source = 19` | 从哪来 |
| `Artifact.training_data = 20` | 训练数据说明（训练产出由我方自动写） |
| `Artifact.license = 21` | 许可（外部导入必填） |
| HTTP `POST /artifacts/import/{domain}`（新增） | 正文工件字节；**来源、训练数据说明、许可、名字必填**（不清楚明写"未知"）；域先校验；不自动启用 |

**三条使用说明**：

1. `origin=imported` 的工件，**界面如实标注"外部导入、训练数据未经本系统核实"**。没有"已核实"状态，这是刻意的。
2. `validated=false` 表示该域没提供校验，文件没经过"能不能用"的检查，请显示出来。
3. `facts` 里是模型文件自带的元数据（如许可、描述）—— 与导入时人填的可能**不一致**（比如文件名 yolo11n、元数据写 YOLOv5n），两者都显示，别只挑一个。

**兼容性**：纯增量。老工件（1.5 之前训练产出的）`origin` 为 `trained`，其余来历字段按任务回填或为空。

## 1.4 —— 2026-09-11

**动机**：视觉域（安全帽检测）落地。它的输入是**图片**，不是测点：不绑 globalId、
不在实时库里、什么时候来由现场决定。原有契约只有"角色 → globalId"一种输入形状（`AI-13 §3` 许下的那一类，现在由真域逼出来了）。

| 改动 | 说明 |
| --- | --- |
| `InputSpec.kind = 5`（新增 string） | `point`（测点）/ `image`（图片）。老版本空串按 `point` 理解；**不是封闭枚举** |
| 纯图片域的 `Binding.roles` 可以为空 | 只有**全部输入都是 `image` 类**的域才放行；测点域少绑一个角色照旧拒 |
| HTTP `POST /infer/{domain}/{binding}`（新增，不在 proto service 里） | 正文图片原始字节；**`X-Captured-At` 拍照时刻必填且带时区**；详见 proto 里 InputSpec 下方的说明 |

**三条使用说明**：

1. **绑定表单**：`image` 类角色不画"选测点"；但**绑定照样要建** —— 台账参数（如置信度阈值）与结论点都挂在绑定上，没有绑定的上传回 404。
2. **拍照时刻没有缺省**：巡检拍完可能半小时后才上传，拿上传时刻当结论时刻就是把"半小时前的违规"记成"现在的违规"。缺了 / 不带时区一律 400。★`Z` 结尾的写法我方已兼容（现场 Python 3.10 的标准库本身不认 `Z`）。
3. **`written=false` 请显示 `write_note`**：只读运行时结论只回给调用方、不进实时库，界面不该让人以为平台上已有这条记录。

**兼容性**：纯增量。老客户端看不到 `kind`，会把图片输入当测点角色画出来 —— 所以**贵方要按 `kind` 渲染绑定表单**，新域才可用。

## 1.3 —— 2026-09-11

**动机**：1.2 里说好"训练执行还没落，故意不给一个永远 pending 的 `StartTraining`"。现在落了。

| 新增 | 说明 |
| --- | --- |
| `rpc StartTraining(StartTrainingReq) returns (MutateRes)` | 建一条待跑任务**立刻返回** id，后台串行跑 |
| `rpc CancelTrainJob(IdReq) returns (MutateRes)` | 取消。★回执**如实说**实际发生了什么，见下 |
| `message StartTrainingReq` | domain / dataset_id / binding（空 = 全域通用模型）/ algo（**由域解释，骨架不枚举**） |

**三条使用说明**（写进 proto 注释了）：

1. **只对声明了 `train` 能力位的域成立**，其余当场拒 ⇒ 请按能力位渲染，别出训练入口；
2. **新工件不自动启用** —— 自动启用等于"训一次就换一次现场模型"，而训练常常只是试试看；
3. ★**"用了多少条样本"要显示出来**：样本记的是时间范围，训练时那段可能已被滚存删掉。
   取不到的不进训练集，但**丢了几条、为什么丢**写在 `TrainJob.message` 与工件 `meta_json` 里。
   「用 200 条训出来的」与「以为用 200 条、实际只用了 3 条」是两个模型，界面上却长得一样。

**`CancelTrainJob` 的回执分三种**，我方不笼统回"已取消"：排队中的当场取消；
**正在跑的只是"请求取消"**（算法不检查取消标志就会跑完当前这次训练才停）；已终态的回"不适用"。

**仍未开**：报告生成（等 LLM 出网定；存储与下载已就位）。

## 1.2 —— 2026-09-11

**动机**：AICloud `C-11` 看完 v4/v5/meter/VFD 四套界面后，给出**七类页面**清单，其中
4~7 四类（标注工作台 / 训练集与知识库 / 训练任务与模型工件 / 片段归档与报告）要我方出端点。

**新增 23 个方法**（纯新增，1.1 的一个字没动）：

| 组 | 方法 |
| --- | --- |
| 标注 | `ListAnnotations` `PutAnnotation` `AnnotateRange` `DeleteAnnotations` `ListLabels` |
| 训练集 | `ListDatasets` `PutDataset` `DeleteDataset` `CopyDataset` |
| 样本 | `ListSamples` `AddSamples` `RemoveSamples` `CopySamples` |
| 工件 | `ListArtifacts` `ActivateArtifact` `DeleteArtifact` |
| 训练任务 | `ListTrainJobs` `GetTrainJob` |
| 片段与报告 | `ListSegments` `PutSegment` `DeleteSegment` `ListReports` `RediagnoseSegment` |

**四条语义在库层落死**（不是靠调用方自觉）：① 标注 ≠ 样本；② 移出 ≠ 删除（`RemoveSamples`
只解关系，原始标注一动不动）；③ 标签不枚举（没有标签表，`ListLabels` 从数据聚合）；
④ 训练是可查询状态的任务，且**终态不可覆盖**。
外加我方一条：**样本上的标签是入集那一刻的快照**，不跟随标注改动。

**质量码订正**：`CONFIG_INCOMPLETE` 出向从 `-1007 QualityOutofService` 改为
**`-1001 QualityConfigError`**。理由见 `C-10 §2`（`-1007` 在 hs 读路径里已是"采集中断"，
与"去补台账"的处置方向正相反）。我方已核：hs 源码从没往数据面写过 `-1001`，这个码是干净的。

**本版故意没开的两件**（不是遗漏）：`StartTraining`（训练执行未落，
**不给一个永远 pending 的口** —— 那比没有更坏）、报告**生成**（等 LLM 出网定；
存储与下载已就位）。

**大对象 HTTP**：新增 `GET /reports/<path>`，与 `GET /artifacts/<path>` **走同一段代码**
（含路径穿越防护 —— 复制一份就等于给它一次退化的机会）。

## 1.1 —— 2026-09-11

**动机**：落第一个算法域（低频振动）时发现的真缺口 —— ISO 10816-3 烈度判级要知道
「机组功率等级 / 刚性还是柔性支承 / 哪一轴是轴向 / 速度口径是不是 RMS」。
这些是**每台机器一份的静态台账**，既没有时刻也没有质量码，**绑不到测点上**；
往实时库里塞一个恒定值的点，就是把台账伪装成测量。

| 改动 | 说明 |
| --- | --- |
| `Binding.params = 8`（新增 `map<string,string>`） | 台账参数的**取值**。骨架只搬运不解释（照 hs 的 `GatewayIdentity.attrs`） |
| `DomainInfo.params = 7`（新增 `repeated ParamSpec`） | 台账参数的**自述**。贵方按它渲染绑定表单 |
| `ParamSpec`（新增消息） | key/display/value_type(含 `enum`)/choices/choice_displays/default/required/unit/description |

**兼容性**：纯新增，老客户端忽略这三处即可正常工作 —— 但**绑定表单会缺字段**，
于是该域的部分结论会落坏质量码（不是猜一个缺省值）。⇒ 贵方要渲染 `params` 才能让新域真正可用。

★与能力位同一条道理：**前端按 `param_specs` 渲染，不按域名写死字段** —— 新增域两侧都不改。

## 1.0 —— 2026-09-10

首版（`AI-11`）。服务名 `aiintegration.AIIntegrationService`，7 个方法：
`GetInfo` / `ListDomains` / `ListBindings` / `PutBinding` / `DeleteBinding` / `SubscribeLogs` / `QueryLogs`。
