# 生成的 gRPC 桩（从 vision.proto 重建，不入库）
vision-infer/app/vision_pb2.py
vision-infer/app/vision_pb2_grpc.py
proto/gen/

# 数据/模型/抓拍（体积大、可重建，见 data/README.md）
data/**
!data/README.md

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# 运行期
config/pipelines.yaml
*.log

# 厂家手册/样本(体积大、非本项目产出;同目录的 md 摘录照常入库)
doc/传感器资料/*.pdf
doc/传感器资料/*.png

# 方案书/客户样张/报告的二进制原件(pdf/pptx)不入库 —— 这几份共 14.8 MB，占了 .git 的几乎全部，
# 且 pdf/pptx 无法 delta 压缩，每改一版就永久堆一个新 blob。结论与依据一律以同名 md 入库，
# 原件留在本地/共享盘。2026-09-10 起停止跟踪(git rm --cached)，文件本身仍在 doc/ 下。
doc/*.pdf
doc/*.pptx
doc/*.docx
# 现场点表等 xlsx 原件同理不入库（2026-09-23）：结论与读出来的事实写进 md。
doc/*.xlsx
# AIIntegration 发布件（make-release.sh 产物，几 MB 的 tar，不入库）
AIIntegration/dist/

# Claude Code：settings.json 与 CLAUDE.md 入库共享；个人本地设置不入库
.claude/settings.local.json
