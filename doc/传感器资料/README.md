# 传感器/DTU 原始资料存档

为《[AI振动诊断_算法侧改造方案](../AI振动诊断_算法侧改造方案_hs供数daqgate建模.md)》选型与能力分级留的**一手依据**。
全部于 **2026-08-12** 从厂家官方渠道取得，避免日后厂家改版/下架后无从对证。

| 文件 | 型号 | 来源 | 用途 |
|---|---|---|---|
| `WT-VB02-485_说明书_语雀原文.md` | **WT-VB02-485**（维特智能） | [语雀 · VB02新版说明书-1021x](https://wit-motion.yuque.com/wumwnr/docs/xufgv4xhnyq9weog) | 规模边界（最大）。含完整寄存器表：阶次点/带谱能量、时域指标、RAWFIFO、采样率/基频/算法模式配置 |
| `USR-SVT10-01B_产品手册_网页原文.md`<br>`USR-SVT10-01B_产品手册_网页全页截图.png` | USR-SVT10-01B（有人物联） | [有人知识库](https://www.usr.cn/wiki/puba/mlhVOHRIY) | 排除依据：全表仅 16 个标量点，无频谱。**寄存器表在网页里是独立渲染层，文本提取会丢，以截图为准** |
| `LH-ST-485_振动传感器说明书_V1.pdf` | LH-ST-485（郎汉德 LonHand，有人代理） | [有人知识库 · 产品手册](https://www.usr.cn/wiki/puba/JLEKIoOAt) | 排除依据：全表仅 9 个寄存器，只有温度 + 三轴速度 |
| `RS-WZ3P-N01-1_频谱型温振变送器_使用说明书_V3.3.pdf` | **仁科 RS-WZ3P**（山东仁科测控） | daqgate 投放 | ★ 能力边界（最全）：唯一能到 **L3b**：加速度谱+速度谱、**测点转速**、**轴承内圈/外圈/滚动体/保持架特征值**、叶片特征值；精度 <1% 读数；统计口径明写 |
| `RS-WZ3-WZ1-N01-1_温振变送器_使用说明书_V3.4.pdf` | 仁科 WZ3 / WZ1（基础型） | daqgate 投放 | 无谱能量，但**口径明写 + 精度 <1% 读数** ⇒ L1+ 无条件成立 |
| `LVIB100_朗尚_宣传手册（无协议手册）.pdf` | 朗尚 LVIB100（上海朗尚） | daqgate 投放 | **反例存档**：规格表写有频点/频带谱能量，但全网只有宣传册、**无协议手册** ⇒ 点表建不出来 |
| `USR-G770_4G-DTU_说明书_V1.0.6.pdf` | USR-G770（有人物联） | [官网直链](https://www.usr.cn/Down/USR-G770_V1.0.6.pdf) | 澄清依据：**它是 4G LTE DTU 不是传感器**，只做 RS232/485 透传，无 Modbus 主动轮询 |

## 抓取备忘（下次要查厂家资料时省时间）

- **usr.cn 的知识库（`usr.cn/wiki/...`）是 JS 渲染 SPA**，任何 HTTP 抓取工具都只能拿到空白页，
  **必须用浏览器打开**。产品页 `usr.cn/Product/xxx.html` 则是普通页面，可直接抓。
- usr.cn wiki 里的 PDF 类文档，真实下载直链是
  `https://www.usr.cn/wiki/service/manualContent/fileDownload?fileId=<32位id>`，
  `fileId` 可从页面里 `<embed>` 的 `src`（pdf viewer 的 `file=` 参数）中取出，取到后可直接下载。
- 维特智能的资料在**语雀**（`wit-motion.yuque.com/wumwnr/docs`），
  振动传感器在「振动传感器 → 震动传感器专业类」下，含 VB01 / **VB02** / VB05 / VB05D 四个系列。
- 有人的振动传感器在知识库「智慧感知设备 → 通用传感器」下，**一共只有 SVT10 和 LH-ST 两款**。

## 尚未取得、需要时再抓

- `LH-ST-LoRa振动传感器说明书V1.pdf`（同系列 LoRa 版，测项不会比 485 版更多，未取）
- `USR-SVT10-01A`（速度型，比 01B 更简，未取）
- 维特 **WT-VB05 / WT-VB05D** 手册 —— 是 VB02 的上位型号，
  若 VB02 的精度（`<F.S±4%`）实测不满足 ISO 分级要求，**这两款是第一顺位的替代候选，届时必查**
- 维特 WT-VB02 的**上位机软件与 SDK**（标定和联调时需要）

---

## ★ 2026-09-17 补：USR-SVT10 官方规格页实取（现场在用的就是这款）

用户告知**现场目前用的是有人（山东济南）的振动传感器**，故补查官方规格。
来源：<https://www.usr.cn/Product/478.html>（产品页是普通页面，可直接抓；wiki 仍是 SPA，见下）。

| 项 | 量程 | 精度 | 本目录此前有记吗 |
| --- | --- | --- | --- |
| 振动速度 | 0–200 mm/s | ±5% @80Hz | 部分 |
| 振动加速度（01B） | ±16 g | ±5% @80Hz | 部分 |
| **位移**（01B） | 0–5000 μm | ★**±10%**（速度的两倍差） | ❌ |
| **频率**（01B） | **10–1600 Hz** | ±5% @80Hz | ❌ |
| 温度 | −40~85 °C | ±1 °C @25°C | 有 |
| ★**频响** | **10–1600 Hz** | —— | ❌ |
| ★**检测周期 / 上传间隔** | **500 ms / 500 ms** | —— | ❌ |

型号差别：**01A = 速度 + 温度**（4 项）；**01B = 速度 + 加速度 + 位移 + 频率 + 温度**（13 项）。

### ★ 新查出的一处口径问题：频带与 ISO 10816-3 对不上

ISO 10816-3 的烈度判级，前提是**速度 RMS 在 10–1000 Hz 带内**（我方
`domains/vibration_lowfreq.py` 的边界表注释里就是这么写的）。
而**这款传感器的频响是 10–1600 Hz**。

若它在整个响应带内算 RMS ⇒ 报出的速度**含 1000–1600 Hz 那段能量**，比 ISO 定义的值**偏高**
⇒ **判级偏严**。

严重程度分情况：速度 = 加速度/ω，高频在速度量纲上本被压制，**一般机器影响小**；
★**但轴承故障时高频冲击能量大，而那正是判级最需要准的时候。**

⇒ 这是继"RMS 还是峰值"之后的**第二个口径问题**，同样**手册没写**。
两条都指向同一个处置：**台架标定**（改造方案 §1.5 已写明"必须台架标定后才能进 L1+"）。

### 另外三条推论

1. ★**低速设备用不了**：频响下限 10 Hz ⇒ **转速低于 600 rpm 的设备，1× 转频落在响应带外**，测不到基频。选型时要记。
2. ★**真传感器应是 2 笔/秒**（500 ms 周期）。而现场实测约 **1 笔/秒**
   —— **又一个"现在接的不是真传感器"的旁证**（已知是 Node-RED + v5 仿真器，见 `AIIntegration/README.md §18.7`）。
3. **位移精度 ±10%，是速度的两倍差** ⇒ 若要用 `vel/disp` 比值估等效频率，分母误差会放大；
   `acc/vel` 那条好些（两边都 ±5%）。

### 两个关键问题：官方规格页**仍然没答**

- ❌ **速度是 RMS（有效值）还是峰值** —— 通篇只写"振动速度"；
- ❌ **±5% 是读数还是满量程** —— 只写"±5% @80Hz"。

★温度那项给的是**绝对精度**（±1 °C），说明厂家在能给绝对值时会给；振动几项给百分比却不说基准。
⇒ 改造方案 §1.5 把有人 SVT10 的 **L1+ 标为 ⚠️ 有条件**，**完全成立**。
★而我方现场那条绑定的 `vel_is_rms` 填的是 `true`，出处是 `C-23`「口径按原项目定为是 RMS」——
**照原项目文档抄的，不是标定出来的。按我方自己写在 `ParamSpec` 里的规矩，它现在没有依据。**

### 抓取备忘（再次验证）

- `usr.cn` 的 **wiki（`/wiki/puba/...`）与下载中心（`/download/index.html`）都是 JS 渲染 SPA**，
  HTTP 抓取只得到空白页 —— 本目录原有的那条备忘**再次被验证是对的**。
  要拿完整寄存器表只能用浏览器打开，或找厂家要。
- **产品页 `usr.cn/Product/478.html` 是普通页面，可直接抓**，规格表在里面。

### ★一处值得注意的落差

厂家宣传页写着这款适用于「**地脚松动、不平衡、对中不良、轴承松动、联轴器撕裂**」等工频故障
—— **厂家自己宣称能判这些**。但它只给标量、不给频谱，按改造方案的档位表这些属于 **L2**。
**宣传与实际能力的落差，正是我方这两天在第③层上绕的那个圈。**
