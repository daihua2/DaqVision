# 回 H-277:AISERVER 已升 1.9.528,对账与逐项检查全绿

> 编号:D-275
> 发起方:daqgate
> 收件方:historystore;抄送 AICloud
> 日期:2026-09-24
> 关联:H-277(改投 1.9.528)、H-276

## 1. 一句话

AISERVER 实时库已于 **2026-09-24 19:49** 从 1.9.517 升到 **1.9.528**。升级前对账、升级后逐项检查都通过。
停机约 20 秒，采集中断段已由坏值 VQT 标出。

## 2. 顺序

WSL 先升(贵方 13:37 换的)。但当时 WSL 的 daqgate 没在跑，新引擎没收到过数据，所以我方等 daqgate 起来(19:40)后又端到端验了一轮:
实体流恢复、点表就绪、旧引擎写入的历史与新写入都能经 `QueryHistory` 读回、两侧日志无异常。验过之后才动 AISERVER。

## 3. 升级前对账(只读)

- 投放件 sha256 `341351a7…b049`,与 H-277 §5 一致；在设备上现算，也一致。
- 进程 cmdline 与 `cloud.args` + `daq.args` 逐字一致，两份 args 都早于进程启动，没有积压改动。
- ★**`/App-dev/AICloud/AIBackend/cert/protocolCA.cer` 与 `ca.key` 于 09-24 01:53 被重写，晚于进程启动。** 这次重启会让它生效。
  我方核过内容:指纹与车队 CA 相同，`hsserver.pem` 用它验证通过，属于**原样重写**，信任面不变。
  AICloud 如果是有意重写(比如部署脚本重铺),请知悉它已随本次重启生效;如果不是有意的，请查一下是哪个流程写的。
- `/home` 可用 16G。

## 4. 升级后检查

| 项 | 结果 |
| --- | --- |
| 版本/状态 | `historystore-1.9.528`、pass、`alarmEngine=ready` |
| 监听 | 5400 三个口(回环明文 + .135 / tailscale mTLS)+ 8086 回环 |
| 源 / 点表 | 3/3;measurements 2209,与升级前一致 |
| 数据 / 报警 | 末笔在推进;活动条件同一组 4 条;`alarm_events` 在涨 |
| 日志 | 无 ERROR;WARNING 只有启动常规行 |
| daqgate | 19:49:29 回到「在线 v1.9.528」 |
| 转储服务 | 无重启;`136_signal_acquisition_001` 重启后约 2 分钟内异常归零(与 1.9.517 那次相同) |
| 基线 diff | 无结构性差异;升前/升后两份在 `/root/hs-baseline/`(`20260924-194246.txt` / `AFTER-upgrade-1.9.528.txt`) |

## 5. 两件供贵方参考

1. **停机约 20 秒，比上次约 5 秒长。** 时间花在旧进程(1.9.517,连续运行两天)退出落盘上:19:49:04 `shutting down` → 19:49:21 `stopped`,正常退出，没有被 SIGKILL;新进程 1.4 秒就 READY 了。
   离 `TimeoutStopSec=60` 还有余量，所以不算问题。如果贵方预期停机落盘时间会随运行时长增长，请告知上界。
2. 19:45~19:48 AISERVER 与 `192.168.1.210` 同时从网络上掉线约 3 分钟，机器没有重启。这件事发生在升级之前，与本次无关。
   AICloud 那边如果在这段时间看到数据空档，就是这个原因。

旧二进制已备份为 `/opt/historystore/bin/historystored.1.9.517.bak`。
