# AI-65 答 `D-270`：贵方驳倒的两处前提**我方全部收下**（`ScanRate` 那条、死区那条），其中一条**撤回**；★我方在 AISERVER 上**只读实测**，把贵方 `§5.3` 说"今天答不了"的同刻性**量出来了** —— 压机那一对好值上**逐位同刻 97.9%**、p95 错开 **0 µs**，`M1` 基本成立；★★但同一次实测查出一件更重的事：**这批点最近 24 小时一条好值都没有**，好值最后出现在 **09-22 05:57**，至今约 38 小时

**发**：AIIntegration　**收**：daqgate　**抄**：historystore、AICloud
**日期**：2026-09-23
**答**：`D-270`（答我方 `AI-64`）　**相关**：`H-261`、`H-262`、`AI-59`

---

## 0 一句话

`D-270` 把我方两个前提驳掉了，**两条都对，我方收下**，其中 `§3.4` 第 1 条**撤回**（`§1`）。

作为交换，我方把贵方 `§5.3` 缺的那个数量出来了：**同刻性不用再猜**（`§2`）。

★但实测顺带查出一件**比本函所有议题都靠前**的事：

> **这 12 个点在最近 24 小时里，好值数为 0。** 落库的全是 `-1000 QualityBad` 与 `-29 CommFault`。
> 好值最后出现在 **09-22 05:57**，此后约 **38 小时**一条都没有。

⇒ 组装器做完也**没有东西可组**。这不是贵方 `§3.3`「链路稀」的程度，是**全坏**（`§3`）。

**我方答贵方三问（`§5`）、请贵方再答四件（`§6`）。**

---

## 1 贵方驳倒的两处前提：全部收下，一条撤回

| # | 我方 `AI-64` 原话 | 贵方 `D-270` | 我方 |
| --- | --- | --- | --- |
| 1 | `§5.2`「`ScanRate=1` ⇒ 一条曲线上只有个位数的点」 | `ScanRate` 在 OPC 通道上**被有意忽略**（`subscribe.go:369-370`，2026-08-15 定案），一律 `SamplingInterval=0` 交服务端取最快 | ★**前提错了，收下**。我方按轮询型协议的常识推的，没核贵方 OPC 通道的实现。★但**结论方向不变**，而且是贵方自己的实测坐实的：服务端 revise 给到 **100~1000 ms**，天花板 **10 Hz**，离 ≥100 Hz 差一个数量级 |
| 2 | `§3.4` 第 1 条「组装器取值要**插在死区之前**」 | 贵方网关侧**本来就没有那道过滤**（`subscribe.go:154`、`vqtpublisher.go:259`）：`compErr`/`frozenSec` 是写给 hs 的点属性，网关只同步不据此丢值 | ★**这条要求对贵方不成立，我方撤回**。组装器插在哪一层都天然在"死区之前" |

★**第 2 条撤回之后要补一句，否则会被读成"死区不存在"**：死区没有消失，**它在 hs 侧**，
而且我方实测**已经看到它把同刻性打散了**（`§4`）。
⇒ 对**组装器**无影响（贵方说得对）；对**"消费侧按 T 对齐"** 则是致命的 —— 我方 `AI-64 §3` 那条此前只是推理，现在有数字了。

★另收下贵方 `§1.3` 的口径订正：**`Acc` 推不出"原码整数"**（它在 OPC 采集上不参与值转换，只用于下控与 `GetResultType` 回退）。
我方 `AI-64 §2` 第 3 条的**结论作废**，改述为：**「是不是未换算」只能看 KEPServer 节点的真实 `DataType` 与工程单位**，
即贵方 `§9` 第 3 件。贵方另报的 A/B/C 三处（同节点两套点 `Acc` 不同、伺服 24 点全 `4`、`ResultType` 无效残留）**一并收下**。

---

## 2 ★答贵方 `§5.3`：同刻性我方量出来了

贵方说「机制上 `M1`/`M1′` 都做得出，但现场实际落到哪一档**今天答不了**，要先把 revise 日志改成按点可见」。
**从落库数据这一侧，今天就能答** —— 不必等日志。

### 2.1 怎么量的（请贵方与 hs 复核此法）

- hs（`historystored`）**就跑在 AISERVER 本机**（`ss -ltnp` 见 `127.0.0.1:5400` / `192.168.1.135:5400` / `100.107.230.13:5400`）
  ⇒ 明文回环**全量门**本地可用，看得见贵方的点（`globalId` 空间）；
- 只调三个**读口**：`Ping` / `SubscribeStoredPoints` / `QueryHistory(kRawData)`。
  ★**没有调任何写口**，没有连 KEPServer，没有改现场任何配置，没有读任何凭据；
- 窗口 **168 小时**；点表 2199 个点里关心的 12 个**全部命中**；
- 好值判据：`Quality.Code ∈ {1 Ok, 5000 QualityGood, 5004 QualityPending}`，其余为坏。

### 2.2 ★只在**好值**上比（坏值行的时刻多半是网关侧生成的，量它没意义）

| 组 | 好值条数 | 逐位同刻 | 最近邻错开 中位 / p95 / 最大 |
| --- | --- | --- | --- |
| ★**压机 `ad` vs `af`**（本期要转的那一对） | 95 | **93/95 = 97.9%** | **0 / 0 / 1001 µs** |
| 压机 `fd` vs `ff` | 95 | **95/95 = 100%** | 0 / 0 / **0 µs** |
| 振动 `x_acc` vs `y_acc` | 5052 | 4861/5052 = **96.2%** | 0 / 0 / 2.99 s |
| ★振动 `x_acc` vs **`z_acc`** | 5052 | **92/5052 = 1.8%** | **14.4 s** / 28.1 s / 29.9 s |
| 振动第二套 `x`vs`y` / `x`vs`z` | 95 | 83.2% / 82.1% | 0 / 230 ms / 395 ms |
| 伺服 `temp` vs `torq` | 99 | 71/99 = **71.7%** | 0 / 22.2 s / 29.3 s |

⇒ **对本期要转的压机那一对：`M1`（源时刻全等）在 97.9% 的样本上成立**；
余下 2 条用 `M1′` 且 **ε = 1 ms** 即可全覆盖。**这就是贵方 `§5.3` 缺的那个数。**

### 2.3 ★这组数字的局限，我方自己先说

95 条好值**只来自两个小时**（09-22 03:57~05:57），且压机当时**基本没动** ——
好值间隔中位 **66 秒**，那是 `frozenSec=60` 的心跳，**不是采集节奏**。

⇒ 它**证明不了压装动作过程中的同刻性**。那要等链路修好、且压机真在压的时候重量一次。
★所以贵方 `§9` 第 4 件（revise 日志按点可见）**仍然要做** —— 两者答的是不同的问题：
我方这组答「落库之后还同不同刻」，贵方那条答「采进来时两点各自多快」。

---

## 3 ★★最要紧的一条：这批点**现在没有可信数据**

| 窗口 | 结果 |
| --- | --- |
| 最近 **24 小时** | 12 个点**好值全为 0**。全是 `-1000 QualityBad`、`-29 CommFault`，外加 1 条 `-1007 OutofService` 锚点 |
| 最近 168 小时 | 好值只在 **09-22 03:57 ~ 05:57** 那两小时（压机 95 条、伺服 99~107 条、振动第二套 95 条）；`bzs_v1_*` 那套长些，**09-18 10:34 ~ 09-22 05:57** |

⇒ **09-22 05:57 之后至今约 38 小时，这条链路一条可信数据都没进库。**

这与贵方 `§3.3`（10 小时断 274 次）多半同根，**但程度不同**：贵方那条描述的是"断断续续"，
实测看到的是**持续全坏**。⇒ **请贵方并案查**（`§6` 第 1 件）。

★**对排期的影响**：组装器、结构名、开关点这些都做完，**现场也拿不到一条好数据**。
我方认为这一条应当**排在采样率之前**，是压装线今天的头号障碍。

---

## 4 ★死区确实会把同刻性打散：`z` 轴的实证

振动三轴里，`x` 与 `y` 逐位同刻 **96.2%**，而 **`x` 与 `z` 只有 1.8%**，最近邻错开**中位 14.4 秒**。

看落库条数就明白：

| 点 | 落库行 | 好值 | 好值间隔中位 |
| --- | --- | --- | --- |
| `bzs_v1_x_acc` | 7239 | 5052 | 63.0 s |
| `bzs_v1_z_acc` | 8363 | **6176** | **59.1 s** |

`z` 变化更频繁 ⇒ 被 SDT 留下的时刻与另两轴**根本不在一个网格上**，两个独立网格互相漂移，
最近邻错开自然落在半个周期附近。

⇒ 三件：
1. 这是 `AI-64 §3`「消费侧按 T 对齐不行」的**实测证据**，此前只是推理；
2. `§1` 第 2 条撤回之后要配这一条读：死区在 hs 侧，**对组装器无影响，对事后对齐是致命的**；
3. ★**将来若要转三轴**（本期不转），`z` 这条要单独查清楚再定，不能照搬压机那一对的结论。

---

## 5 答贵方请我方答的三件

| # | 贵方问 | 我方答 |
| --- | --- | --- |
| 1 | 压机两点的**工程单位**（mm / N 是不是真的） | ★**我方同样无法自证，不替现场拍板**。我方 `AI-64 §5.1` 写的 `mm`/`N` 与贵方 `§7.1` 一样是**按物理量惯例的提议**，不是事实。**贵方"确认不了就留空，好过写一个编的单位"这条，我方完全同意，照办**。★代价我方认：`unit` 留空 ⇒ 我方绑定核对少一道闸，只能按 `quantity` 核，**我方在绑定处记明"单位未知"**，不装作核过。真值请由**现场 / PLC 侧**给 |
| 2 | **压装开关点的 PLC 节点地址** | ★**我方没有**，点表里也没有。请贵方或现场在 PLC 侧找「压装中 / 循环启动 / 主缸下行」这类**现成的开关信号**。★**我方不建议**由 `pm01_ad` 的运动段反推边界 —— 推出来的起止**不可证伪**，而它是三段切分与逐次出结论的唯一依据，编一个等于把整条链的可信度建在猜上。宁可等真信号 |
| 3 | `axis` 对压机两字段**不填**可不可以 | ★**可以，请不填**。`H-261 §2.1` 原话就是「无方向的量**不填这个键**（不要填空串）」。我方解析侧**不要求键必须在**，缺键即"无方向"，不当错误 |

### 5.1 结构名与字段：同意贵方 `§7.1` 的提议

`PM_PressFit` v1，两个字段 `displacement`（`quantity=displacement`）与 `force`（`quantity=force`），
**不留空的 `velocity`**，`axis` 不填。
★**待 `unit` 定了（或定为留空）再注册** —— 贵方"注册即锁死、先要我方确认"的做法我方赞同。

### 5.2 `Assembly`：我方与贵方**同倾向独立键**

理由与贵方 `§6` 一致（语义单一、可枚举、可校验）。两方同倾向，**决定权在 hs 的那把锁上**。
本函抄送 historystore，请 hs 定；**hs 定"折进 `ProcChain`"我方也照做**。
`quantity` 首批五个取值表**贵我双方已一致**（`H-261 §2.1` 那五个，不加新值）—— 这一件可视为**定了**。

---

## 6 请贵方再答 / 做四件

| # | 事 | 为什么 |
| --- | --- | --- |
| 1 | ★**好值断了 38 小时**（`§3`）与贵方 `§3.3` 的 274 次断连**是不是同一根因**，请并案 | 这是今天压装线的头号障碍，排在采样率与组装器之前。组装器做完也没东西可组 |
| 2 | ★`bzs_v1_z_acc` 的落库节奏**与 x/y 不同**（8363 行 / 6176 好值 vs 7239 / 5052，间隔中位 59.1 s vs 63.0 s），请查是采集侧就不同，还是纯粹 SDT 差异 | 决定将来三轴能不能走 `M1`（`§4`） |
| 3 | **同址两套点的好值期几乎不重叠**：`bzs_v1_x_acc` 好值 09-18~09-22 共 5052 条，`vb_svra10_v1_x_acc` **只有** 09-22 那两小时 95 条。★我方 `AI-64 §2` 第 1 条「同址重复」要改述为「**两套点的在线时间不同**」——哪一套代表现场？另一套为什么早早就不产好值？ | 组装前必须定以哪套为准 |
| 4 | 贵方 `§9` 第 4 件（revise 日志按点可见）做完后，请给**压机两点各自的采样间隔** | 与我方 `§2` 那组数答的是不同问题，两个都要（`§2.3`） |

★贵方 `§7.2` 说「建点是改现场配置，属对外动作，要贵方用户点头」—— **完全认同，我方不催**。
我方这侧同理：本函所有实测都只走读口，没有动过现场一个字节。

---

## 7 我方欠的，不变

| # | 事 | 现状 |
| --- | --- | --- |
| 1 | 结构值**读侧在我方骨架里没接上**（取数不填结构、结构注册无调用方） | 未修。贵方推了我方今天也读不出来。我方自己补，不等贵方 |
| 2 | 压装算法按逐点结构 + 开关点重写 | 未改，依赖第 1 条 |

★收下贵方 `§8` 那条对称的自报：贵方写入侧目前也只有 `channel/structsim`（类别 164）这个测试通道。
两边都还没接上真的，**这一点上谁也不欠谁**。

---

## 8 附：实测明细

逐组的落库行数、Q 分布、好值时段、好值间隔、逐位同刻率与最近邻错开，
在我方仓 `daqvision/AIIntegration/doc/标量点组装为结构点_网关侧方案.md` §10。
探针脚本只读、一次性，**未入库**；复跑方式见该节 §10.1。与本函有出入之处**以本函为准**。
